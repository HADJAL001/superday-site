# Координация — superday-site (лендинг SUPER DAY)

> Документ для синхронизации между агентами проекта «Оптимизатор дня» /
> SUPER DAY. Прежде чем править этот репозиторий — прочитай раздел «Границы».

## Участники: OS 3.0 и OS 3.3

В этом репозитории параллельно работают **две модели**, и с волны 11 их подписи
разведены, чтобы по журналу было видно, кто что вёл:

- **OS 3.3** — функциональные волны веб-клиента и лендинга: логика дня, парсеры,
  календарь, панель, язык интерфейса, слой подсказок. Волны 1–11.
- **OS 3.0** — оформление и кинематика: палитра, типографика, анимации сборки
  дня. Волна 12.

Разделы волн 1–10 писались до разделения подписей — тогда репозиторий вела одна
роль, и они помечены OS 3.3. Правило совместной работы прежнее: **перед правкой
`git status`, коммитить свои файлы поимённо**, чужой незакоммиченный код не
захватывать и не откатывать.

## Границы ответственности

- **Этот репозиторий (`HADJAL001/superday-site`, домен `superday.fun`)** —
  публичный маркетинговый лендинг и веб-приложение `app.html`.
- Это **не** приложение и **не** бэкенд. Здесь нет auth, billing, API-логики.
  Всё, что касается ядра (ai-core), голоса (voice-core), инфраструктуры
  (auth/billing/infra) — в отдельных репозиториях и НЕ дублируется тут.
- Хостинг: **GitHub Pages** (файл `CNAME` = `superday.fun`). `netlify.toml`
  лежит как запасной конфиг, но живой деплой — Pages. Значит **Netlify Forms
  здесь не работают** — для сбора почт нужен внешний эндпоинт.

## Что сделано в P0-проходе (2026-07-25, OS 3.3)

Приоритет P0 из дизайн/продукт-аудита — «спасти предзапуск»:

1. **P0.1 — Реальный сбор e-mail списка ожидания.**
   Раньше форма открывала `mailto:hello@superday.fun` и почты **не собирались**.
   Теперь форма шлёт `fetch POST` на внешний эндпоинт (Formspree).
   - Конфиг — константа `WAITLIST_ENDPOINT` в начале основного `<script>` в
     `index.html`.
   - **Пока константа пустая — работает старый mailto-фолбэк (без регресса).**
   - Как активировать: см. раздел «Активация» ниже.

2. **P0.3 — OG-картинка для соцсетей.** `assets/og.png` (1200×630) —
   правильный формат превью вместо квадратного логотипа. Подключена в
   `og:image` и `twitter:image` (абсолютным URL, как требуют скраперы).

3. **P0.2 — Приватная веб-аналитика (без cookie-баннера).** Сниппет добавлен в
   `<head>` `index.html` закомментированным, с маркером `ACTIVATE-ANALYTICS`.
   Аккаунт может завести только человек-основатель — см. «Активация».

## Активация (шаги для основателя / ответственного агента)

### 1. Сбор почт (Formspree — бесплатно, без бэкенда)
1. Зайти на https://formspree.io → создать форму (email = ящик, куда падают
   заявки, напр. `hello@superday.fun`).
2. Скопировать endpoint вида `https://formspree.io/f/xxxxxxx`.
3. В `index.html` найти `const WAITLIST_ENDPOINT = ""` и вставить туда URL.
4. Запушить. Всё — почты копятся в дашборде Formspree + приходят на ящик.
> Альтернативы без смены кода-контракта: любой сервис, принимающий
> `POST` с полем `email` и отвечающий `2xx` (Getform, Basin, свой Supabase
> Edge Function). При желании перейти на Supabase — согласовать с ответственным за бэкенд
> (инфра), т.к. это уже общий бэкенд.

### 2. Аналитика (GoatCounter — бесплатно, приватно, без cookies)
1. Зарегистрировать код на https://www.goatcounter.com (напр. `superday`).
2. В `index.html` раскомментировать блок `ACTIVATE-ANALYTICS` и подставить код.
> Аналог: Cloudflare Web Analytics или Plausible — тот же принцип, один тег.

## Правила для всех, кто трогает этот репозиторий

- Никаких фейковых отзывов, рейтингов, счётчиков подписчиков — только правда.
- Не коммитить секреты. Formspree/аналитика используют **публичные** id — это ок.
- Домен `superday.fun` уже привязан; HTTPS выпущен. Не отвязывать.
- Правки — аддитивные; палитра премиум-графит + приглушённое золото (см. `:root`).
- После правок: node-проверки (баланс CSS `{}`, `new Function` для JS,
  `JSON.parse` ld+json), затем `git push origin main`, проверить live.

## Что сделано в P1/P2-проходе (2026-07-25, OS 3.3)

- **Hero-хук на «снятие тревоги»** — заголовок «Дел слишком много? Начни с
  главного», подзаголовок про выгрузку голосом. (P1)
- **Ценовой якорь** ≈26 ₽/день на карточке Standard. (P1)
- **APK → GitHub Releases** — 82 МБ вынесены из репо в релиз `android-beta`,
  ссылка скачивания ведёт на CDN GitHub. Репозиторий облегчён. (P2)
- **`security.txt`** (RFC 9116) в `.well-known/`. (P2)
- **Офлайн web-MVP — `app.html`** (P2, крупная веха):
  - Рабочий планировщик прямо в браузере: ввод текстом/голосом (Web Speech API),
    раскладка по матрице Эйзенхауэра (тумблеры «Срочно»/«Важно»), маршрут дня,
    прогресс, `localStorage` (ключ `superday_tasks_v1`), без бэкенда.
  - Полностью офлайн: `sw.js` v2 кэширует `app.html`; PWA-ярлык в манифесте.
  - Связка с лендингом: пункт nav «Веб-версия» + hero-CTA «Попробовать в
    браузере». В `sitemap.xml`.
  - **Это фронтенд-демо, не общий бэкенд.** Данные лежат только в браузере
    пользователя. Если появится синхронизация/аккаунты — это уже зона бэкенда (auth/billing/infra)
    (инфра/auth), согласовывать отдельно, `app.html` тут только клиент.
- **Голосовой ассистент в `app.html`** (2026-07-25, доп. веха):
  - Режим «Свободные руки» (Web Speech API, `continuous`): приложение слушает
    и просыпается на имя-обращение, затем принимает задачу/команду.
  - **Пользователь сам задаёт имя приложения** (поле «Как обращаться…»,
    хранится в `localStorage` ключ `superday_wake`) + всегда работают запасные
    фразы («супер день», «окей супер» и т.п.).
  - Голосовые команды: добавить задачу (с флагами «срочно»/«важно»),
    «заметка …», «прочитай план», «прочитай заметки», «убери выполненные».
    Ответ голосом (`speechSynthesis`, ru-RU) + короткий сигнал (WebAudio).
  - Раздел «Заметки» с локальным хранением (`superday_notes_v1`).
  - Всё офлайн, только браузер. Это по-прежнему клиент, не бэкенд.
- **Правка и резервная копия в `app.html`** (2026-07-25):
  - Инлайн-правка текста задач/заметок по клику (Enter — сохранить, Esc —
    отмена, пустой текст не затирает).
  - Экспорт в файл `superday-backup-ГГГГ-ММ-ДД.json` (задачи + заметки +
    имя-обращение) и импорт **со слиянием**: дубликаты по тексту пропускаются,
    существующее не затирается. Это ручной перенос между устройствами —
    **не** облачная синхронизация (та остаётся зоной бэкенда (auth/billing/infra)).
  - `sw.js` поднят до `superday-v3` (обновляет офлайн-копию `app.html`). При
    следующих правках `app.html` — снова поднимать версию `CACHE`.

- **Блок «поделиться» после подписки** (2026-07-25): в success-карточку списка
  ожидания добавлена кнопка «Поделиться SUPER DAY» (`navigator.share` → буфер
  обмена → textarea-фолбэк, ссылка `superday.fun`). **Честно, без обещаний
  двигать очередь** — просто приглашение показать другу веб-версию. Закрывает
  прежний долг «реферальная ссылка».

## Харденинг безопасности и доступности (2026-07-25, OS 3.3)

- **CSP + заголовки безопасности** через `<meta http-equiv>` (единственный путь
  на GitHub Pages) в `index.html` и `app.html`. Ключевое:
  `upgrade-insecure-requests`, `object-src 'none'`, `base-uri 'self'`.
  `script-src`/`connect-src` намеренно допускают `https:` — **чтобы будущая
  активация Formspree/аналитики (раздел «Активация») не сломалась**. Инлайн-стили
  и скрипты требуют `'unsafe-inline'`. Проверено headless Chrome: **0 CSP-нарушений**.
  ⚠️ При активации внешнего сервиса ничего в CSP менять не нужно (https уже разрешён);
  если захочется ужесточить до конкретных доменов — правь `connect-src`/`script-src`.
- **`referrer=strict-origin-when-cross-origin`** и **`color-scheme:dark`** (нативные
  контролы и скроллбар в тёмной гамме) в обоих файлах.
- **`prefers-reduced-motion`** теперь уважается и в `app.html` (был только в
  `index.html`) — универсальный сброс анимаций/переходов для вестибулярной
  чувствительности.
- `sw.js` поднят до `superday-v6`. При правках `app.html`/`index.html` —
  снова поднимать `CACHE`.
- **PWA-иконки исправлены** (`superday-v7`): manifest раньше объявлял только
  `1024×1024 purpose:any` — браузер слабо предлагал установку, Android-маска
  срезала логотип. Добавлены `assets/icon-192.png`, `icon-512.png` (any),
  `icon-maskable-512.png` (лого ~70% в безопасной зоне на фоне `#0A0B0E`) и
  лёгкий `apple-touch-icon-180.png` (заменил 600 КБ `logo.png` в `<link>`).
  Иконки в précache SW. Генерились из `assets/mark.png` через Pillow.

## Волна 1 — UX/безопасность данных/голос (2026-07-26, OS 3.3)

Правки только в `app.html` (веб-версия SUPER DAY), аддитивно. `sw.js` → `superday-v8`.

- **Честное уведомление о приватности голоса** — под голосовой панелью: задачи и
  заметки не покидают браузер, но само распознавание речи обрабатывается облачным
  сервисом браузера (Web Speech API → напр. Google). Без обмана пользователя.
- **Автобэкап-страховка** — `autoBackup()` пишет снимок `superday_autobackup` в
  localStorage при каждом `save()`/`saveNotes()`. Плюс баннер `#backupRemind` в
  databar: напоминает сделать файловую копию, если экспорта не было >7 дней
  (`superday_last_export` ставится при успешном экспорте).
- **Версионирование схемы данных** — `superday_schema` (SCHEMA=1) + миграция при
  старте нормализует поля задач без потерь. Задел, чтобы будущие изменения формата
  не роняли данные.
- **Карточка «Начни с этого»** (`#focusCard`) — показывает ОДНУ первую задачу по
  приоритету квадранта + кнопку «Готово». Снимает паралич выбора.
- **Доступность** — `:focus-visible` кольца (золото), клавиатура на чекбоксах
  задач (Space/Enter), `aria-live` на статусах, `aria-label` на чекбоксах.
- **Премиальный чёткий голос** — `speak()` теперь выбирает лучший русский голос
  (приоритет Microsoft Online/Natural → Google → именованные женские ru),
  корректно ждёт `onvoiceschanged`, спокойный тон `rate=0.98 pitch=0.96`.
  Работает в самой веб-версии SUPER DAY (`app.html`).
- Проверено: баланс CSS `{}` (138/138), `new Function` по JS — OK, headless
  Chrome — 0 CSP-нарушений и 0 runtime-ошибок.

## Волна 2 + «работает сразу» (2026-07-26, OS 3.3)

Фидбэк основателя: «ничего не понятно, надо чтоб сразу можно было назвать дела
и всё работало сразу». Правки в `app.html` и `index.html`, `sw.js` → `superday-v11`.

- **`app.html`** — недельная статистика + стрик (`superday_stats_v1`), шаблоны дня
  (`superday_templates_v1`, слияние без дублей), упрощённый интро/плейсхолдер/
  подсказка композера, честная деградация голоса вне HTTPS (микрофон не молчит —
  показывает, что голос заработает по защищённому адресу; текст работает всегда).
- **`index.html`** — устранён разрыв «витрина ↔ приложение»: главная золотая
  CTA «Открыть планировщик — работает сразу» ведёт в `app.html`; телефон-макет
  в hero стал ссылкой на `app.html` с подсказкой «Это живое приложение —
  нажми и попробуй →». Список ожидания — вторичная кнопка.
- Проверено: CSS `{}` 265/265, JS/JSON-LD OK, headless Chrome 0 ошибок; live 200,
  контент подтверждён curl.

## Волна 3 — «время у дел» + живые напоминания (2026-07-26, OS 3.3)

Правки только в `app.html`, аддитивно. `sw.js` → `superday-v12`. SCHEMA → 2.

- **Время у задачи прямо из текста и голоса.** Пишешь/говоришь «позвонить маме
  **в 15:00**», «тренировка **в 7 утра**», «обед **в полдень**» — парсер
  `parseTime()` вынимает время (ЧЧ:ММ), чистит текст, ставит дело в расписание.
  Понимает `15:00`, `в 9`, `в 7 утра/вечера/дня/ночи`, `полдень/полночь`, `к 18`.
  Протестирован на 19 фразах (в т.ч. ложные: «заплатить 3.14 за кофе» → без времени).
- **Бейдж времени у каждого дела** (`.ttime`) — кликом задаёшь/меняешь/убираешь
  время (`editTaskTime`, гибкий ввод). Статусы: `soon` (<60 мин, золото),
  `now` (±10 мин, пульс), `over` (просрочено, красный). В фокус-карточке «Начни
  с этого» — время и «через N мин».
- **Маршрут дня сортируется по времени** (`planOrder`): дела со временем идут
  первыми в хронопорядке, затем — по приоритету квадранта; выполненные вниз.
  Колонка времени в маршруте (`.pt`), время в экспортируемом тексте и в озвучке
  плана.
- **Живые напоминания** (тумблер в `#remindBar`, ключ `superday_remind`): пока
  открыта вкладка, при наступлении времени дела — уведомление (`Notification`,
  если разрешено), голос (`speechSynthesis`) и подсветка; помечается
  `remindedOn` (раз в день). Тик раз в 30 c, окно срабатывания 45 мин.
  **Честно в UI:** работает при открытой вкладке; фоновые push при закрытом
  приложении — вне фронтенда (`needs_backend`, зона бэкенда: auth/billing/infra, отдельные репозитории).
- **Данные не теряются:** миграция схемы v2 добавляет `time`/`remindedOn` без
  потерь; экспорт `version:2`, импорт-слияние и шаблоны дня переносят `time`.
- Проверено: CSS `{}` 192/192, `new Function` по JS — OK, headless Chrome — 0
  CSP/runtime-ошибок; `parseTime` из живого файла — 13/13 контрольных фраз.

## Волна 4 — таймбоксинг + «Итог дня» (2026-07-26, OS 3.3)

Правки только в `app.html`, аддитивно. `sw.js` → `superday-v13`. SCHEMA → 3 (+`dur`).

- **Длительность у дела из текста и голоса.** `parseDuration()` понимает «на 30
  минут», «1 час», «полтора часа», «1 ч 30 мин», «2ч20мин», «пол часа», «на час».
  Парсится ДО времени (чтобы «на 5 минут» не читалось как 05:00). Тест 11/11 +
  ложные срабатывания отсечены («купить молоко» → null). Бейдж `.tdur` у дела:
  клик — задать/изменить/убрать (`editTaskDur`). Хелперы `fmtDur`/`fmtDurShort`/
  `endTime` (интервал с переходом через полночь: 23:30+60 → 00:30).
- **Интервалы в маршруте** — `.pt` показывает `15:00–16:30`, длительность рядом с
  текстом (`.pd`). **Детект наложений** (`findOverlaps`): если конец дела заходит
  на начало следующего по времени — оба помечаются `⚠ наложение` (класс `.clash`).
- **Сводка дня** (`#daySummary`, `renderDaySummary`): сумма запланированных минут,
  прогресс-бар против **бюджета дня** (`superday_daybudget`, дефолт 480 мин, правится
  кликом `editDayBudget`), предупреждение о перегрузе, счётчик дел без оценки.
- **«Итог дня»** (`#dayVerdict`, `renderVerdict`) — контекст + фраза-вердикт по
  РЕАЛЬНОЙ нагрузке: «Мда, денёк был не из лёгких — но мы справились» /
  «Супер лёгкий день — и по-другому быть не могло» / «День тяжёлый, перенеси часть».
  Учитывает бюджет, наложения, число дел, погоду/пробки/топливо.
- **Контекст дня (`superday_dayctx`, посуточный) — только настоящие данные:**
  - **Погода** — авто через **Open-Meteo** (бесплатно, без ключа, CORS) по
    геолокации; при отказе/оффлайне — ручной ввод температуры. Ничего не выдумываем.
  - **Пробки** — честный самоотчёт (лёгкие/средние/адские, чип по кругу).
  - **Топливо/дорога** — ручной ввод суммы (₽) или литров.
  Контекст и вердикт попадают в текст «поделиться планом» и в озвучку плана.
- **Данные не теряются:** миграция схемы v3 добавляет `dur` без потерь; экспорт
  `version:3`, импорт-слияние и шаблоны дня переносят `dur`.
- Проверено: `new Function` по JS — OK (65 КБ), CSS `{}` 219/219, headless Chrome —
  0 CSP/runtime-ошибок, DOM содержит `#dayVerdict`/`#daySummary`; `parseDuration`
  из живого файла 11/11, `endTime` (вкл. переход через полночь) — верно.

## Волна 4.1 — граница дня / перенос незавершённого (2026-07-26, OS 3.3)

Правки только в `app.html`, аддитивно. `sw.js` → `superday-v14`.

- **Мягкий перенос дня** (`checkNewDay`/`showRollover`, ключ `superday_lastday`):
  при открытии в новый календарный день, если остались дела — баннер `#rolloverBar`
  «Новый день»: «Перенести N незавершённых, убрать выполненные» или «Оставить как
  есть». **Ничего не удаляется без явного нажатия.** При переносе: выполненные
  убираются, у оставшихся сбрасывается `remindedOn` (напомним заново), `superday_dayctx`
  очищается (контекст — на новый день). Существующим пользователям баннер ложно не
  показывается (первый заход просто синхронизирует `lastday`).
- Проверено: JS OK (68 КБ), CSS `{}` 230/230, headless 0 ошибок, live 200, sw v14.

## Волна 4.2 — недельный итог по нагрузке (2026-07-26, OS 3.3)

Правки только в `app.html`, аддитивно. `sw.js` → `superday-v15`.

- **Журнал нагрузки дней** (`superday_daylog`): при каждой отрисовке «Итога дня»
  (`recordDayLog` из `renderVerdict`) сохраняется, **как дался день** —
  `{load, done, total}` за дату. Хранится ~60 последних дней (старые вытесняются).
  `dayVerdict` теперь возвращает `{phrase, ctx, load, done, total}`.
- **Недельные столбцы подсвечиваются по тяжести** (`loadBand`/`LOAD_COL`): 🟢
  лёгкий день / 🟡 в меру / 🔴 тяжёлый. В тултипе столбца — выполнено + метка
  нагрузки (`done/total`). Стрик недели теперь показывает не только «сколько
  закрыл», но и «как дался день».
- **Строка-итог недели** над `#statsFoot`: «Неделя по нагрузке: 🟢 N лёгких ·
  🟡 M в меру · 🔴 K тяжёлых». Считается только по дням, где есть запись.
- Данные считаются **только в этом браузере** — честно, без облака.
- Проверено: `new Function` по JS — OK (~70 КБ), CSS `{}` 230/230, headless
  Chrome — 0 CSP/runtime-ошибок; live 200, sw v15, контент подтверждён curl.

## Волна 5 — таймер фокуса (таймбокс в действии) (2026-07-26, OS 3.3)

Правки только в `app.html`, аддитивно. `sw.js` → `superday-v16`.

- **Длительность дела стала действующей.** В карточке «Начни с этого» — кнопка
  **«▶ Фокус»**: запускает таймбокс на длительность дела (`dur`) или 25 мин по
  умолчанию. Замыкает главный цикл продукта: «выгрузи → начни с главного →
  **делай под таймер**».
- **Панель `#focusTimer`** (`renderFocusTimer`): кольцо-прогресс (conic-gradient
  по `--p`), обратный отсчёт `MM:SS`, кнопки **Пауза/Продолжить · +5 · Стоп ·
  Готово**. «Готово» отмечает дело выполненным и учитывает в статистике
  (`recordCompletion`).
- **Финиш таймбокса:** мягкий трёхнотный аккорд (WebAudio, self-contained),
  голос (`speechSynthesis`), `Notification` (если разрешено) + подсветка-тост.
- **Состояние переживает перезагрузку** — ключ `superday_focus`, счёт по
  **wall-clock** (`endTs`), а не по тикам. Если время истекло, пока вкладка была
  закрыта, — при возврате **тихо** показываем финиш-панель (без автозвука/спама).
- **Честно:** работает при открытой вкладке. Фоновый сигнал при полностью
  закрытом приложении — вне фронтенда (`needs_backend`, зона бэкенда: auth/billing/infra, отдельные репозитории).
- Проверено: `new Function` по JS — OK (~77 КБ), CSS `{}` 252/252, headless
  Chrome — 0 CSP/runtime-ошибок, DOM содержит `#focusTimer`; логика таймбокса
  (пауза/резюм/+5/финиш, `fmtClock`) — 13/13 на mock-времени.

## Волна 6 — тихий режим (один переключатель на весь звук) (2026-07-26, OS 3.3)

Правки только в `app.html`, аддитивно. `sw.js` → `superday-v17`. Схема данных не менялась.

- **Проблема.** Звук шёл из **пяти независимых мест**: сигнал «слушаю» и голосовые
  ответы голосового режима, чайм и голос напоминаний, чайм и голос финиша
  таймбокса. Заглушить всё одним движением было нельзя — приходилось выключать
  напоминания целиком (теряя пользу) или глушить систему.
- **Кнопка «🔊 Звук включён / 🔇 Тихий режим»** (`#muteToggle`) — в панели
  напоминаний, рядом с тумблером 🔔. Состояние в `superday_mute`, переживает
  перезагрузку; при включении **обрывается уже начатая фраза** `speechSynthesis`
  (иначе «выключил» ощущается сломанным).
- **Единая точка правды** — глобальные `isMuted()`/`setMuted()`/`paintMute()`
  объявлены на верхнем уровне скрипта, поэтому видны из всех IIFE-модулей
  (голос, напоминания). Все 5 источников звука спрашивают `isMuted()`.
- **Тихо ≠ слепо.** Напоминания и финиш таймбокса продолжают работать: тост на
  экране остаётся, системное `Notification` приходит с `silent:true` вместо того
  чтобы исчезнуть. Пропустить дело из-за тихого режима нельзя.
- **Голосовой режим честен:** включённый при «тихо» пишет в статусе
  «🔇 Тихий режим включён — отвечаю текстом, без голоса»; `speak()` молчит, но
  вызывает колбэк, поэтому цепочка команд не рвётся.
- Проверено: `new Function` по JS — OK (78 КБ), CSS `{}` 255/255, headless
  Chrome — 0 CSP/runtime-ошибок, кнопка в DOM; логика тихого режима из живого
  файла — **11/11** (дефолт, персист, обрыв речи, перерисовка кнопки, молчание
  чайма и голоса, возврат звука, переживание перезагрузки). Live: 200, `sw-v17`.

## Волна 7 — разгрузка дня / перенос на завтра (2026-07-26, OS 3.3)

Правки только в `app.html`, аддитивно. `sw.js` → `superday-v18`. **SCHEMA → 4** (+`defer`).

- **Проблема.** С волны 4 приложение умело сказать «⚠ Перегруз на 2 ч» — и на
  этом останавливалось. Предупреждение было тупиком: чинить перегруз человек
  должен был вручную, удаляя дела (то есть теряя их).
- **Поле `defer`** (`"ГГГГ-ММ-ДД" | null`, миграция v4 без потерь): дело
  помечено «на завтра». Оно **остаётся в своём квадранте** (приглушённое, с
  меткой), но выходит из сегодняшнего маршрута, бюджета, «Начни с этого» и
  напоминаний. Единый фильтр — `isToday()`/`isDeferred()` в `planOrder`,
  `render`, `renderFocus` и тике напоминаний.
- **Кнопка «→ завтра / ← сегодня»** (`.tdef`) у каждого дела — перенос и возврат
  в одно нажатие. Счётчики квадрантов и прогресс «N из M» считают **только
  сегодняшние** дела, иначе цифры врали бы про сегодня.
- **«🪄 Разгрузить день»** (`showUnloadPlan`) — появляется в сводке только при
  перегрузе. Подбор (`pickUnload`): жертвуем сначала наименее приоритетным
  квадрантом (4→1), внутри — сперва делами **без назначенного времени** (нет
  договорённости с миром), затем более длинными (один перенос освобождает
  больше). Сначала **показывается список** с обоснованием и остатком бюджета,
  применяется только по кнопке «Перенести на завтра». Ничего не удаляется.
  Если перегруз дают дела без оценки времени — так и говорим, а не гадаем.
- **Возврат автоматический:** `clearStaleDefer()` при старте и в `checkNewDay()`
  снимает наступившие метки — завтра дела просто снова сегодняшние.
- Экспорт `version:SCHEMA`, импорт сохраняет `defer`; все точки создания дела
  пишут `defer:null`.
- Проверено: `new Function` по JS — OK (84 КБ), CSS `{}` 275/275, headless
  Chrome — 0 CSP/runtime-ошибок; юнит-тесты логики из живого файла **18/18**
  (границы `defer`, порядок жертв, подбор под разные бюджеты, авто-возврат);
  интеграционный прогон в headless с реальным DOM — **20/20** (исключение из
  маршрута, счётчики, панель разгрузки, применение и ручной возврат).
  Live: 200, `sw-v18`, `SCHEMA = 4` на проде.

## Волна 8 — календарное измерение: регулярные дела + любая дата (2026-07-26, OS 3.3)

Правки только в `app.html`, аддитивно. `sw.js` → `superday-v19`. **SCHEMA → 5**
(+`repeat`, +`doneOn`).

- **Проблема.** После волны 7 приложение знало ровно два дня: «сегодня» и «завтра».
  Регулярных дел не существовало — зарядку, планёрку, таблетки человек вбивал заново
  каждое утро (обходной путь — ручные «шаблоны дня», которые надо не забыть загрузить).
  Получалось наоборот тому, ради чего продукт сделан: **самое повторяемое дело требовало
  самого большого ручного труда.** Перенос тоже упирался в «завтра», хотя поле `defer`
  технически хранит любую дату.
- **Поле `repeat`** (`{days:[0..6]}` | `null`, 0=Вс). Регулярное дело **не порождает
  копии** — оно живёт одним объектом, а в «не свои» дни недели просто не считается
  сегодняшним: тот же механизм, что у переноса. Копии-инстансы плодили бы объекты и
  ломали статистику.
- **Единая точка правды — `isToday()` = `!isDeferred() && !isOffDay()`.** Одна правка, и
  повтор автоматически учли `planOrder`, счётчики квадрантов, сводка и бюджет дня,
  «Начни с этого» и тик напоминаний — без новых веток фильтрации. Тик напоминаний
  переведён с `isDeferred` на `isToday` (иначе регулярное дело звонило бы в чужой день).
- **Поле `doneOn`** — день отметки «выполнено». `resetRepeats()` на старте и в
  `checkNewDay()` снимает галочку у регулярного дела, если она стоит с прошлого дня.
  Отметка выполнения свелась к общему `markDone()` (было три места, где `done=true`
  ставился вручную: чекбокс, карточка «Начни с этого», «Готово» таймера фокуса).
- **`purgeDone()` — защита от потери правила.** «Убрать выполненные» и кнопка переноса
  дня раньше делали `filter(t => !t.done)` и **стёрли бы регулярное дело**. Теперь
  регулярное остаётся и сбрасывается в невыполненное; в `clearAll` `confirm` честно
  пишет, сколько правил повтора будет удалено.
- **Повтор прямо из речи и текста.** `parseRepeatPhrase()` понимает «каждый день»,
  «ежедневно», «по будням», «по выходным», «каждый вторник». Порядок разбора в
  `addTask` и в голосовом модуле: **повтор → длительность → время** (сначала снимаем
  «каждый день», иначе «день» мешает разбору времени). Список оборотов нарочно узкий,
  ложные отсечены тестами: «купить хлеб на день рождения», «отчёт за понедельник»,
  «планы на выходные», «совещание на целый день» → повтора нет.
- **Бейдж `.trep`** у дела: `＋повтор` / `🔁 каждый день` / `🔁 пн ср пт`. Клик —
  `editTaskRepeat` (prompt по образцу `editTaskTime`/`editTaskDur`, пусто = снять повтор).
  Дело в чужой день недели получает класс `.offday` (приглушено, как перенесённое) и
  не попадает в маршрут, бюджет, фокус и напоминания.
- **Перенос на любую дату.** `parseDay()` понимает «завтра», «послезавтра»,
  «понедельник» (ближайшее будущее), «через 3 дня», «28.07» (прошлое → следующий год),
  ISO; только будущее и не дальше года. `fmtDefer()` рисует подпись «→ завтра» /
  «→ пн 28» / «→ 15 авг». **Быстрый путь не изменился:** клик по `→ завтра` = завтра;
  диалог выбора дня открывается кликом по уже перенесённому делу. `setDefer` принимает
  дату (старый булев вызов поддержан).
- **Сохранность данных.** Всё живёт в `localStorage`, а браузер вправе его вычистить
  (Safari/iOS — после ~7 дней без визитов). `askPersistentStorage()` запрашивает
  `navigator.storage.persist()` — но только когда данных уже ≥3 дел, чтобы Firefox не
  показывал системный запрос случайному посетителю. Если постоянство не выдано, панель
  копий честно пишет: «Браузер может очистить данные при долгом простое».
- **Данные не теряются:** миграция v5 добавляет `repeat`/`doneOn` без потерь; экспорт
  `version:SCHEMA`, импорт и шаблоны дня переносят `repeat`; все точки создания дела
  пишут `repeat:null, doneOn:null`.
- Проверено: CSS `{}` 291/291, `new Function` по JS — OK (99 КБ); юнит-тесты логики,
  вытащенной **из живого файла** в Node — **78/78** (парсеры повтора и дат, форматтеры,
  матрица `isToday` × повтор × перенос, `resetRepeats`, `purgeDone`, `planOrder`,
  ложные срабатывания); интеграция в headless Chrome с реальным DOM — **28/28**
  (добавление фразой, исключение из счётчиков/маршрута/фокуса, «Убрать выполненные»
  не убивает регулярное, перенос на завтра → на дату → возврат, правка повтора
  диалогом, `doneOn`, шаблон дня); миграция **v4 → v5 без потерь — 14/14**;
  0 CSP/runtime-ошибок. Live: 200, `superday-v19`, `SCHEMA = 5` на проде.

## Волна 8.1 — онбординг первого экрана (2026-07-26, OS 3.3)

- **Проблема.** Новый человек попадал сразу в композер и матрицу. Первая победа
  (закрыть дело) не была никак подсказана, а именно она решает, останется ли человек.
- **Карточка `#onboard`** над композером: «Три шага — и день разложен» — впиши дело ·
  скажи голосом (можно сразу «в 15:00» или «каждый день») · нажми «Готово» в карточке
  «Начни с этого». Клик по шагу 1 ставит фокус в поле ввода.
- **Показывается ровно один раз и только новичку** (ключ `superday_onboard_v1`): если
  задачи уже есть, ключ ставится молча и карточка не появляется никогда. Исчезает сама
  после первой добавленной задачи — **объяснение не переживает первую победу**.
- После первого дела — разовая подсказка через `toast`: в какой квадрант дело попало
  и что флажки 🔥/⭐ и перетаскивание это меняют.
- Подсказка композера дополнена примером повтора: «зарядка **каждый день** в 7 утра».

## Волна 9 — день в календарь (`.ics` с будильниками) (2026-07-26, OS 3.3)

Правки только в `app.html`, аддитивно. `sw.js` → `superday-v20`. **Схема данных не
менялась (SCHEMA = 5)** — новых полей у задачи нет, значит и миграции нет.

- **Проблема.** Самое честное ограничение продукта было записано прямо в UI:
  «Работает, пока открыта эта вкладка». Напоминания, таймер фокуса и голос живут только
  во вкладке; фоновый push при закрытом приложении требует сервера (`needs_backend`,
  зона бэкенда (auth/billing/infra)). Ограничение было **тупиком**: человек читал его и ничего не мог сделать.
- **Решение без бэкенда.** Системный календарь уже установлен на каждом устройстве и
  умеет будить телефон сам. `buildICS()` собирает день в файл `.ics` (RFC 5545) с блоками
  `VALARM` — это **настоящие фоновые напоминания без единого запроса к серверу**.
- **Что уезжает в календарь.** Дело со временем → `VEVENT` (`DTEND` = время + `dur`, без
  оценки — 30 мин; переход через полночь считается через `Date`, а не сложением строк).
  Регулярное дело (волна 8) → **одно** событие с `RRULE` (`FREQ=DAILY` при 7 днях, иначе
  `WEEKLY;BYDAY=MO,WE,FR`) — правило вместо копий. Перенесённое (`defer`) → событие на дату
  переноса. Выполненное разовое не уезжает; выполненное **регулярное** уезжает (правило живёт
  дальше). Дела без времени не теряются: одно событие на весь день (`DTSTART;VALUE=DATE`)
  со списком в `DESCRIPTION` и **без будильника** — звонить не о чем.
- **Два будильника** на дело со временем: `TRIGGER:-PT10M` («Через 10 минут: …») и
  `TRIGGER:PT0S` («Пора: …»), `ACTION:DISPLAY`.
- **Повторная выгрузка обновляет, а не дублирует.** `UID` = `sd-<id>@superday.fun`
  стабилен, `SEQUENCE` растёт (счётчик `superday_ics_seq`) — календарь понимает, что
  версия свежее, и правит то же событие вместо создания второго.
- **Время «плавающее»** (`DTSTART:20260726T150000`, без `Z` и без `TZID`): личный план
  читается в локальном времени смотрящего, и это избавляет от блока `VTIMEZONE`. Это
  валидный RFC 5545, а не упрощение.
- **`nextRepeatDate()` не звонит в прошлое:** если сегодня «свой» день, но время уже
  прошло, старт регулярного дела ставится на следующий подходящий день — иначе часть
  клиентов срабатывает будильником прямо в момент импорта.
- **Свёртка строк по октетам, а не по символам** (`icsFold`/`icsOctets`): лимит 75 байт,
  кириллица — 2 байта, эмодзи — суррогатная пара. Наивная резка сделала бы строки длиннее
  нормы, а резка по байтам порвала бы сам символ. Экранирование `\` `;` `,` и перевода
  строки — по RFC (`icsEsc`).
- **Две точки входа, один обработчик:** кнопка «📅 В календарь» в тулбаре и
  кнопка-ссылка `#icsFromRemind` **внутри самой честной подписи** про ограничение —
  выход даётся там же, где объясняется проблема. На мобильных сначала пробуем
  `navigator.canShare({files})` → `navigator.share` (на iOS файл уходит прямо в
  «Календарь»), иначе — обычное скачивание `superday-ГГГГ-ММ-ДД.ics` по тому же паттерну,
  что резервная копия. Итог сообщается честными числами: сколько дел со временем,
  сколько регулярных, сколько ушло заметкой на день.
- CSP править не понадобилось — `blob:`/`data:` уже разрешены под экспорт копии.
- Проверено: юнит-тесты логики **из живого файла** в Node — **84/84**
  (`C:\Users\HADJAL\w9_unit.js`: экранирование, свёртка ≤75 октетов с round-trip
  развёрткой для кириллицы и эмодзи, `DTEND` через полночь, дефолт 30 мин, `RRULE`,
  `nextRepeatDate`, перенос, выполненные, событие на день, стабильность `UID` и рост
  `SEQUENCE`, баланс `BEGIN/END`, только CRLF); интеграция в headless Chrome с реальным
  DOM — **30/30** (`w9_integration.js`: перехват `URL.createObjectURL`, MIME и имя файла,
  обе точки входа дают один файл, путь «Поделиться» на мобильном, пустое состояние даёт
  сообщение, а не пустой файл); CSS `{}` 293/293, `new Function` по JS — OK (108 КБ);
  чистая загрузка по http — **0 ошибок консоли/CSP**. Образец файла для ручной проверки
  в календаре — `C:\Users\HADJAL\superday-preview.ics`.

## Волна 10 — тихий экран и правая панель (2026-07-26, OS 3.3)

Правки только в `app.html`, аддитивно; `sw.js` → `superday-v21`. **Схема данных не
менялась (SCHEMA = 5)** — новых полей у задачи нет, значит и миграции нет. Новый ключ
`superday_panel_v1` хранит только состояние интерфейса (последний открытый раздел
панели) и данных пользователя не касается.

- **Проблема.** Приложение доросло до 15 секций в одном вертикальном свитке: интро,
  онбординг, композер, напоминания, голос, ролловер, фокус, таймер, матрица, маршрут,
  неделя, шаблоны, заметки, тулбар, копии. Каждая волна честно добавляла ценность — и
  каждая добавляла на первый экран ещё один блок текста. Хуже того, **каждое дело
  рисовалось дважды**: интерактивной строкой в матрице и строкой «только чтение» в
  маршруте. Это и создавало ощущение «всё напичкано информацией».
- **Решение — два слоя.** Главный экран отвечает на один вопрос («что делать сейчас»):
  композер → «Начни с этого» → **Сегодня** → прогресс. Всё остальное уехало в правую
  выдвижную панель. Ни одна возможность не удалена — сложность не исчезает, она
  переносится на слой, который человек открывает добровольно.
- **Панель `#panel`** — 9 разделов-аккордеонов (открыт один за раз): «Разбор по
  важности» (матрица), «Напоминания и звук», «Голос», «Неделя», «Шаблоны дня»,
  «Заметки», «Данные и календарь», «Как это работает», «Под капотом». Секции
  **физически переехали теми же DOM-узлами** — `renderStats()`, `renderNotes()`,
  `renderTemplates()`, экспорт/импорт и `.ics` работают без единой правки. Это и
  сделало крупный редизайн почти безрисковым.
- **Поведение панели.** `Esc` закрывает; фокус-ловушка внутри, при закрытии фокус
  возвращается на кнопку; фон получает `inert` + `aria-hidden`. Открытие делает
  `pushState("#panel/<раздел>")` — **аппаратная «Назад» на телефоне закрывает панель,
  а не уводит со страницы**. Свайп от правого края открывает, свайп вправо внутри —
  закрывает. На экранах ≥1024px у правого края всегда виден рельс из 9 иконок: клик
  открывает панель сразу нужным разделом. Свёрнутые разделы подписаны живыми
  значениями («выключены», «2 заметки», «копия 3 дня назад»), а точка на кнопке
  зажигается, только когда внутри правда что-то требует внимания (напоминания
  выключены при делах со временем либо давно не было копии).
- **Класс `.open` ставится после `void offsetWidth`, а не в `requestAnimationFrame`:**
  кадр мог прийти уже после закрытия панели и вернуть её на экран.
- **Единый список дел.** Построение строки вынесено в общую `taskRow(t, opts)` — ровно
  тот же DOM и те же обработчики. Маршрут строит строки через `taskRow(t,{plan:true})`:
  время, длительность, повтор, перенос, удаление и переименование правятся **прямо в
  маршруте**; вместо ручки перетаскивания — цветная точка важности, клик по ней
  открывает панель на матрице и подсвечивает это дело (`spotTask`). Матрица осталась
  местом перетаскивания: `quadUnder()` на `elementFromPoint` работает и внутри панели,
  у её краёв добавлена автопрокрутка (`edgeScroll`). Призрак перетаскивания (`z-index:50`)
  остаётся выше панели (`40`) — это закреплено самотестом. Бейджи вынесены в `.tmeta`
  второй строкой: иначе на узком экране текст дела сжимался в столбик из двух букв.
- **Три бага, вскрытых переездом (все были и раньше):**
  1. `q.querySelector(".empty")` в `render()` попадал не в подсказку квадранта, а в
     бейдж `＋повтор` внутри первого дела (у бейджей тот же класс `empty`) — подсказка
     «пока пусто» висела при непустом квадранте, а бейдж пропадал. Исправлено на
     `:scope > .empty`, закрыто самотестом.
  2. У `.focustimer` не было правила `[hidden]{display:none}` — карточка таймера фокуса
     висела на главном экране всегда, показывая «00:00». Правило добавлено.
  3. На мобильном `.composer .row1{flex-direction:column}` превращал `flex:1 1 260px`
     поля ввода в **высоту 260px**. Добавлен `flex:0 0 auto` в медиазапросе.
- **Встроенные самотесты — кандидат №1 прошлой итерации, закрыт этой волной.**
  `app.html?selftest=1` открывает раздел «Под капотом», прогоняет **41** проверку на
  живом коде и пишет итог в `document.title` (`OK 41/41` / `FAIL n/41`) — headless-прогон
  читает заголовок. Кнопка «Прогнать самотесты» делает то же вручную, `window.__selftest()`
  доступна из консоли. Группы: парсеры, матрица и маршрут, календарь, форматтеры и
  **«целостность DOM»** — тест вытаскивает из собственного текста скрипта все
  `getElementById` и проверяет, что каждый id находится в разметке. Это ровно тот страж,
  которого требовал переезд половины приложения в другой контейнер. Тесты чистые:
  глобальный `tasks` подменяется только на время прогона и возвращается в `finally`.
  Раздел «Под капотом» показывает версию схемы, число дел/заметок/шаблонов, занятое
  место, постоянство хранилища, дату последней копии и имя офлайн-кэша.
- Проверено: юнит-тесты прошлых волн **без правок зелёные** — `w8_unit.js` 78/78,
  `w9_unit.js` 84/84; встроенные самотесты **41/41**; интеграция в headless Chrome
  **по http** (не `file://`) — `C:\Users\HADJAL\w10_integration.js`, **65/65**
  (переезд секций, открытие/закрытие кнопкой, `Esc` и затемнением, «Назад», аккордеон
  и запоминание раздела, рельс, хеш-роутинг, интерактивный маршрут, точка важности →
  подсветка в матрице, добавление дела при открытой панели, `.ics` из панели, призрак
  выше панели, диагностика); CSS `{}` 342/342, `new Function` по JS — OK (132 КБ);
  чистая загрузка — **0 ошибок консоли/CSP**. Скриншоты 390×844 и 1440×900 —
  `C:\Users\HADJAL\shots\`.

## Открытые долги (ещё не сделаны)

- Активация сбора почт (Formspree endpoint) и аналитики — шаги для человека
  (см. раздел «Активация»); код готов, ждёт публичных id. CSP уже совместим.
- ~~HTTPS-сертификат домена~~ **ЗАКРЫТО (2026-07-26).** Причина застревания:
  домен был задан только файлом `CNAME` в репо (legacy build) → GitHub
  регистрировал домен (HTTP работал), но объект `https_certificate` в
  `GET /pages` был `null` — выпуск Let's Encrypt **не инициировался**; крон,
  пинчивший файл, этого не запускал. Фикс: через API `gh api -X PUT .../pages`
  снять домен (`{"cname":null}`) → дождаться билда → вернуть (`-f cname=...`).
  Это форсировало свежую DNS-верификацию: cert прошёл `authorization_pending`
  → `approved` за ~1 мин. Затем `https_enforced=true` (boolean через JSON, не
  `-f`). Итог: валидный Let's Encrypt на apex+www, `https://superday.fun` 200,
  www→301. Durable-крон HTTPS удалён. Голос (Web Speech API) теперь в secure
  context — работает.
- Упрощение hero-эффектов — по желанию, не блокер.

## Следующее по ценности (кандидаты на волну 10+)

Ранжировано по «ценность на единицу кода», всё — чистый клиент, без бэкенда.
(Экспорт в `.ics` закрыт волной 9; встроенные самотесты — волной 10.)

1. ~~Утренний и вечерний ритуалы~~ — закрыто волной 19 («Намерение дня» + вечерний
   итог с аффирмацией).
2. ~~Импорт `.ics` обратно~~ — закрыто волной 21 (см. ниже).
3. ~~Контексты жизни~~ (работа / дом / здоровье) + баланс недели — закрыто волной 22
   (см. ниже).
4. **Клавиатура, одна рука, полный a11y-проход** — условие «стандартного» приложения.
   **Следующий кандидат.**
   Волна 10 закрыла половину: фокус-ловушка, `Esc`, `inert`, возврат фокуса, «Назад».
5. ~~i18n (RU/EN)~~ — закрыто волной 11 (RU/EN/ES/DE/FR/ZH по `navigator.languages`).

Вне этого репозитория (`needs_backend`, зона бэкенда (auth/billing/infra)): аккаунты, синхронизация,
фоновый push, командный режим, AI-дебаты.

**Приём оплаты из любой точки мира** (запрос основателя, 2026-07-26) — тоже **не сюда**:
это billing, общий бэкенд и юридическая обвязка. Здесь можно будет только показать цену
и кнопку, ведущую на checkout. Задача для бэкенда (отдельный репозиторий): платёжный провайдер с международным
покрытием + локальный способ для РФ (карты РФ у Stripe/Paddle не проходят), значит
минимум два маршрута оплаты, выбор по стране плательщика, и подписка через сторы для
мобильных сборок. До появления такого эндпоинта лендинг **не должен обещать оплату** —
правило «никаких обещаний, которых продукт не выполняет» остаётся в силе.

## Волна 11 — заявка: язык по настройкам браузера (план, код не начат)

Запрос основателя (2026-07-26): **язык должен определяться по настройкам браузера
автоматически** + улучшения интуитивного понимания. Языки утверждены:
**RU, EN, ES, DE, FR, ZH**. Скоуп: `app.html` **и** `index.html`.

Раздел написан до начала работы — чтобы i18n (кандидат №6 списка выше) не начали
параллельно и чтобы решения были видны заранее.

**Замер объёма:** 147 текстовых узлов, 34 атрибута (`placeholder`/`title`/
`aria-label`), ~693 строковых литерала в JS, пять парсеров на русских регулярках
(`parseTime`, `parseDuration`, `parseRepeat`, `parseRepeatPhrase`, `parseDay`).
`Intl` не используется нигде: локаль зашита (`u.lang="ru-RU"` в четырёх местах,
`toLocaleString("ru-RU")`, `DAY_SHORT`, ручная русская `plural(n, forms)`).

**Архитектурное решение — внешний слой, а не разметка ключами.** Скрипт
`app.html` — классический `<script>` без модуля и без охватывающего IIFE (девять
локальных IIFE — только привязки обработчиков), поэтому все функции приложения
глобальны. Значит i18n живёт в отдельном файле `i18n.js`, а в `app.html`
добавляется одна строка подключения. Так правка не спорит с параллельной работой
в этом файле — это и было решающим доводом (заявка писалась, когда волна 10 ещё
лежала незакоммиченной).

- **Детект:** `?lang=` → сохранённый выбор (`superday_lang_v1`) →
  `navigator.languages` с нормализацией (`ru-RU`→`ru`, `zh-CN`→`zh`) → `en`.
- **Русский — нулевая цена:** RU уже в HTML; при русском браузере словарь не
  грузится вовсе. Остальные словари ленивые, грузится ровно один.
- **Перевод живого DOM** + `MutationObserver` — покрывает и строки, рождающиеся
  в JS (тосты, карточки, маршрут), куда статическая разметка не достаёт.
- **Ввод понимается на любом языке, а не только на языке интерфейса:** обёртки
  над глобальными парсерами нормализуют вход (`at 3pm`→`в 15:00`,
  `every day`→`каждый день`), логика парсеров не дублируется и их тесты остаются
  в силе. Формат хранения `"HH:MM"` не меняется — локализуется только показ
  (`Intl.DateTimeFormat`, `Intl.PluralRules` вместо русской `plural`).
- **Голос без правок файла:** обёртки над конструкторами
  `SpeechSynthesisUtterance`/`SpeechRecognition` проставляют `lang` по локали.
- **Переключатель языка вставляется в панель волны 10 программно** — её же
  паттерном `.pgroup`/`.pg-head`/`.pg-body`, без правок разметки.
- **Защита текущей аудитории:** у части русских пользователей браузер английский,
  поэтому при первом автопереключении показывается одна плашка «Switched to
  English · Русский?» с кнопкой возврата; выбор запоминается.
- **Лендинг:** тот же движок + `hreflang` на шесть языков и `x-default`,
  `og:locale:alternate`, обновление `sitemap.xml`. Статические `/en/index.html`
  для SEO — кандидат следующей волны, не сюда.

**Честный риск:** словарь по строкам ломается, если текст переформулируют —
строка выпадет и останется русской (деградация видимая, но не ломающая). Ловится
самотестом покрытия. После стабилизации волны 10 — вторая фаза: перевод словаря
на устойчивые ключи `data-i18n`.

**Проверка (сверх обычного ритуала):** русский набор юнит-тестов парсеров до и
после правок должен дать те же результаты — это доказательство отсутствия
регрессии; `app.html?selftest=1` — тот же `pass/total`; headless-прогон с
подменённым `navigator.languages` по каждому из шести языков.

**Интуитивность — тем же внешним слоем (`ux.js`), в коде этого нет (проверено
грепом):** живой разбор ввода чипами под полем (сейчас синтаксис объясняется
текстом подсказки — станет виден до добавления), кликабельные примеры вместо
пустого экрана, отмена в тосте для «Очистить всё» и «Убрать выполненные»
(сейчас `toast()` умеет только текст), предсказание квадранта вместо
«→ решим за тебя», клавиши `/` и `?`.

## Волна 12 — платина и глубокая синь + кинематика сборки дня (2026-07-26, OS 3.0)

Запрос основателя: «весь дизайн платиново, с тёмно-синими значками и контейнерами,
текст золотом или как лучше, шрифт красивее», «по-детски написано — нужно нормальное
описание» и «должны быть 8K-эффекты при генерации графика дня». Правки в `app.html`
и `index.html`, `sw.js` → `superday-v22`. **Схема данных не менялась (SCHEMA = 5)** —
волна чисто оформительская, ни одного поля у задачи не добавлено.

Волна 11 (i18n) не тронута: файлы `i18n.js` / `i18n/` агента OS 3.3 не редактировались.

### Материалы: два металла разведены по смыслу

Главное решение, которое не даёт интерфейсу превратиться в «ёлку»: **платина = действие,
золото = состояние, синий = опора.** Блестит ровно то, на что надо нажать.

- `:root` переписан на платиново-синюю систему: `--bg #0B1020`, `--ink #0A1122`,
  `--surface #111A2E`, `--border #243252`, `--platinum #DEE4EE`, `--grad-plat`
  (металлический блик), `--on-accent #080D18` — тёмный текст на светлых кнопках.
  Волосяные линии `--hair` перекрашены из золотых в холодные синие.
- Зашитые мимо токенов цвета вычищены механически: `#0E0F13` → `var(--ink)` (18 мест),
  `#1A1509`/`#141414`/`#0F1014` → `var(--on-accent)`, серые `rgba(32,35,43…)`,
  `rgba(18,19,24…)` и родня → синие тройки с сохранением альфы. Теперь смена палитры —
  правка одного блока, а не поиск по файлу.
- Плашки значков (`.pg-ic`, рельс панели) — тёмно-синий контейнер с внутренним бликом
  и `filter:saturate(.85)`, чтобы эмодзи не спорили с металлом.
- Платиновыми стали первичные действия: «Добавить», «Готово», `.ro-primary`, `.ul-ok`,
  загрузка шаблона, `.ds-unload`, `.ft-done`. Золото осталось за включёнными
  тумблерами, «скоро» у времени, галочкой и серией.
- На лендинге класс `.btn-gold` переименован в `.btn-plat` (15 мест, CSS + разметка) —
  имя перестало врать о цвете. Попутно вскрылся старый баг: правило `nav.links a`
  специфичнее классов кнопок и красило текст CTA в приглушённо-серый; на золоте это
  читалось плохо, на платине почти исчезло. Исправлено адресными правилами.

### Типографика

`Cormorant Garamond` → **`Playfair Display`** (600/700/800, кириллица полная): выше
контраст штриха, держит мелкий кегль, читается «дорого». Семейство вынесено в токен
`--font-display` — оба файла ссылаются на него, а не на строку с именем шрифта.
Добавлены `font-variant-numeric:tabular-nums` (цифры перестали «прыгать» при
пересчёте), отрицательный трекинг заголовков, `text-rendering:optimizeLegibility`.

### Восемь эффектов «генерации графика дня»

Вопрос основателя был «8K-эффекты, но какие?». Ответ: не картинки и не библиотека —
**чистая векторная кинематика на композиторных свойствах**. «8K» здесь означает
буквально: ничего растрового, всё остаётся резким на любом DPI, а холст частиц
масштабируется по `devicePixelRatio`.

1. **Каскад строк** — маршрут проявляется по очереди (сдвиг + расфокус, шаг 45 мс):
   взгляд успевает прочитать порядок дня, а не получает его вспышкой.
2. **Луч-сканер** — один платиновый проход по карточке «Сегодня» в момент пересборки.
3. **Дыхание приоритета** — точка важности первой строки коротко расцветает.
4. **Живая кромка** — по периметру «Начни с этого» ходит блик; сделан маской
   `border-box ⊕ content-box`, то есть настоящая рамка в 1 px, а не подложка под текст.
   Угол крутится через `@property --edge`; без поддержки — статичный блик, не поломка.
5. **Роллинг чисел** — «сделано N из M» прокручивается снизу, изменение видно боковым
   зрением.
6. **Жидкий прогресс** — ширина едет с пружиной, по заливке идёт блик.
7. **Искры** — закрытое дело выбрасывает горсть платиново-золотых частиц из самой
   галочки. Canvas, ≤48 частиц на выброс и ≤260 в воздухе, буфер по `devicePixelRatio`
   (потолок 3 — выше глаз не видит, а площадь растёт квадратично).
8. **Наклон** — карточка «Начни с этого» поворачивается за курсором (≤3.4°), только
   `hover:hover and pointer:fine`: на пальце это мешает, на клавиатуре бессмысленно.

Инженерные решения, которые стоит помнить:

- **Кинематика не знает о логике, логика не знает о кинематике.** Весь модуль — блок
  `fx*` в одном месте; `renderPlan()` зовёт `fxPlanBuilt()`/`fxRoll()`, строка дела —
  `fxSpark()`. Вырезать блок можно целиком, приложение продолжит работать молча.
- **Анимация никогда не задерживает данные:** сначала DOM верен, потом проявляется.
- **Каскад проигрывается только при смене состава маршрута** (сигнатура из id и флага
  «выполнено»), иначе каждая мелкая перерисовка дёргала бы экран.
- Перезапуск анимаций — через `void offsetWidth`, а не `requestAnimationFrame`: тот же
  урок, что с открытием панели в волне 10.
- Вкладку свернули посреди полёта частиц — `rAF` замирает и искры остались бы висеть;
  на `visibilitychange` холст гасится.
- `prefers-reduced-motion: reduce` глушит **всё**, включая искры: это проверяется
  тестом, а не подразумевается.

### Тексты: убрана «детскость»

Переписаны без уменьшительно-успокаивающей интонации и без смены обращения (весь
остальной интерфейс на «ты» — смешивать нельзя, поэтому новые формулировки нейтральны):
заголовок «Просто назови свои дела — увидишь, что делать первым» → «Список дел
превращается в порядок действий»; «→ решим за тебя» → «→ приоритет определится сам»;
«Добавь задачи — и здесь появится спокойный маршрут дня» → «Пока пусто. Первая
добавленная задача соберёт маршрут дня»; онбординг, подсказка композера, подзаголовок
маршрута, лид лендинга. Вердикты дня («Мда, денёк был не из лёгких — но мы справились»)
заменены на факты: «Нагрузка выше обычной. Сделано 3 из 7; остаток разумно перенести
на завтра».

### Проверено

- Юнит-тесты прошлых волн **без единой правки**: `w8_unit.js` 78/78, `w9_unit.js` 84/84.
- Встроенные самотесты — **41/41** (`app.html?selftest=1`, итог в `document.title`).
- Новый харнесс `C:\Users\HADJAL\w12_check.js` (headless Chrome по http, не `file://`):
  каскад навешивается на все строки со ступенчатой задержкой 45 мс, холст искр создан
  по DPR, заголовок реально Playfair, кнопка платиновая, у значка синяя плашка,
  после клика по галочке на холсте **375 непрозрачных пикселей**, при
  `prefers-reduced-motion: reduce` — каскада нет и искр **ноль**, ошибок консоли **0**.
  Два урока харнесса записаны в код: headless без флагов
  `--disable-renderer-backgrounding` и родни не композитит кадры (`rAF` молчит, и любой
  canvas выглядит «сломанным»), а частицы, рождённые ниже сгиба, честно отсекаются —
  элемент надо сперва подвести в видимую область.
- CSS `{}`: app 394/394, index 265/265; `new Function` по JS и `JSON.parse` по обоим
  `ld+json` — OK. Лендинг: **0 ошибок консоли**.
- Скриншоты — `C:\Users\HADJAL\shots\w12-desktop.png`, `w12-mobile.png`, `w12-landing.png`.

### Новые и изменённые строки — для автора i18n (волна 11)

Чтобы не вычитывать diff: «Планировщик дня · без регистрации», «Список дел
превращается», «в порядок действий.», лид интро, «Три шага до готового плана» и три
шага онбординга, «Настройка не требуется · данные остаются в этом браузере»,
плейсхолдер «Задача — например, «позвонить маме»…», подсказка композера,
«→ приоритет определится сам», подзаголовок «Сегодня», «Пока пусто. Первая добавленная
задача соберёт маршрут дня», пять фраз вердикта дня в `dayVerdict()`. На лендинге —
лид hero, `hero-note`, `phone-open`, строка манифеста и `m-sub`.

## Стратегия: удержание и монетизация (2026-07-26)

Запрос основателя. Раздел — **стратегия, а не обещание в UI**: до появления реального
checkout лендинг оплату не обещает (правило проекта в силе).

### Удержание — что делается здесь, клиентом, без бэкенда

1. **Петля «вечер → утро»** (следующая волна, план готов): вечерний итог задаёт главное
   дело на завтра, утренний ритуал с него начинается. Вход и выход из дня — то, чего
   продукту не хватает, чтобы стать привычкой, а не инструментом.
2. **Календарные якоря `.ics`** — единственный честный способ вернуть человека при
   закрытом приложении без сервера: системный календарь будит телефон сам.
3. **Честная серия вместо очков.** Никаких фейковых счётчиков — серия считается по
   фактическим дням и её можно потерять.
4. **Peak-end (Канеман):** день заканчивается «победой дня», а не списком невыполненного.
5. **Закрытие незавершённого (Зейгарник):** вечером каждое незакрытое дело явно
   переносится или отпускается — снимается ровно та фоновая тревога, ради которой
   продукт и нужен.
6. **Возврат после пропуска без укора:** «пропуск — это нормально, начнём с одного дела».
   Стыд убивает трекеры привычек быстрее, чем скука.
7. **Локальность данных как ценность доверия** — сначала доверие, потом деньги.

### Монетизация — зона бэкенда (auth/billing/infra), здесь только цена и кнопка на checkout

- **Freemium, где платят не за снятые искусственные ограничения, а за то, что реально
  стоит денег:** синхронизация между устройствами, фоновый push при закрытом
  приложении, история глубже 60 дней, командный режим, AI-разбор. Это защищает от
  оттока и не требует тёмных паттернов.
- **Момент показа цены — после прожитой ценности:** попытка открыть на втором
  устройстве; третья неделя подряд с закрытыми ритуалами; ручной экспорт `.ics` или
  бэкапа («хочешь, чтобы это делалось само?»).
- **Два платёжных маршрута** (merchant-of-record для мира + локальный для РФ), выбор по
  стране плательщика; для мобильных сборок — подписки через сторы.
- **Экспорт данных бесплатен всегда** — и этика, и юридическая гигиена, и лучший
  маркетинг для этой аудитории.

## Следующая волна (план готов, код не начат)

**Ритуалы дня** — утренний и вечерний, по 2 минуты; новый ключ `superday_ritual_v1`,
SCHEMA остаётся 5. Полный план — `C:\Users\HADJAL\.claude\plans\stateless-dazzling-metcalfe.md`.
Дальше по списку: контексты жизни (потребует SCHEMA 6) и импорт `.ics` обратно.

## Волна 11 — язык по настройкам браузера + слой интуитивности (2026-07-26, OS 3.3)

Запрос основателя: **язык определяется по настройкам браузера автоматически**
(RU, EN, ES, DE, FR, ZH) и «чтобы приложению не было равных по интуитивному
пониманию». Сделано целиком **внешними файлами**: в `app.html` добавлены две
строки подключения, в `index.html` — одна. Схема данных не менялась (SCHEMA = 5),
формат хранения времени `"HH:MM"` тоже — локализуется только показ.
Коммит `0500df6`, `sw.js` → `superday-v23`.

### Почему внешний слой, а не разметка ключами

Скрипт `app.html` — классический `<script>` без модуля и без охватывающего IIFE
(локальные IIFE только вешают обработчики), поэтому **все функции приложения
глобальны**: `window.parseTime`, `window.render`, `window.save`, `window.tasks`.
Значит перевод и подсказки делаются снаружи, а площадь правки в общем файле —
две строки. Это и позволило вести волну параллельно с оформительской волной 12
без единого конфликта.

### `i18n.js` — движок языка

- **Детект:** `?lang=` → сохранённый выбор (`superday_lang_v1`) →
  `navigator.languages` с нормализацией (`ru-RU`→`ru`, `zh-Hans`→`zh`) → `en`
  запасным (язык вне списка получает международный английский, а не русский).
- **Русский — нулевая цена:** словарь не грузится вовсе, RU уже в разметке.
  Проверено тестом: при русском браузере запросов за `i18n/*.js` — ноль.
- **Перевод живого DOM** (текстовые узлы + `placeholder`/`title`/`aria-label`/
  `alt`) и `MutationObserver` — он достаёт до строк, которые приложение рисует
  в рантайме: тосты, карточки дел, маршрут. На время своей записи наблюдатель
  отключается, накопленные записи сбрасываются — цикла не возникает.
- **Часы по локали:** `Intl.DateTimeFormat` — английский получает `3:00 PM`,
  остальные `15:00`. Хранение не трогается.
- **Ввод понимается на языке пользователя** — обёртки над пятью парсерами:
  фраза приводится к русской форме (`at 3pm` → `в 15:00`, `every day` →
  `каждый день`, `um 15:00`, `à 15:00`, `每天`), дальше работает оригинал.
  Логика парсеров не продублирована, их тесты остались в силе. Если оригинал
  не понял фразу **и** до нормализации — возвращается результат на исходной
  строке, чтобы нормализация не могла испортить текст дела русскими вставками.
- **Голос без правок файла:** патчится сеттер `lang` у
  `SpeechSynthesisUtterance`/`SpeechRecognition` — четыре зашитых `"ru-RU"`
  больше не перебивают локаль.
- **Переключатель языка** вставляется в панель волны 10 программно её же
  паттерном `.pgroup`/`.pg-head`/`.pg-body`; панели нет — садится в шапку.
- **Плашка для русских с английским браузером:** при первом автопереключении
  один раз показывается «Language set to… · Русский?» с кнопкой возврата.
- **Самотест покрытия `?i18ntest=1`** (и `window.__i18ntest()`): перечисляет
  непереведённые русские строки. Непереведённое **остаётся русским**, а не
  пустым — деградация видимая, но не ломающая.

Словари: `i18n/{en,es,de,fr,zh}.js` — приложение, `i18n/site.{en,es,de,fr,zh}.js`
— лендинг (грузится только там, по `data-scope="site"` на теге подключения).
Приложение не тянет текстов лендинга и наоборот.

### Лендинг

`hreflang` на шесть языков + `x-default`, `og:locale:alternate`. Отдельных
файлов на язык нет — все ссылки ведут на тот же адрес с `?lang=`; статические
`/en/index.html` для SEO остаются кандидатом следующей волны.

### `ux.js` — слой интуитивности

1. **Живой разбор ввода.** Пока человек печатает, под полем зажигаются чипы:
   что понято как время, длительность, повтор. Раньше синтаксис объяснялся
   текстом подсказки — теперь он виден **до** добавления. Чипы зовут те же
   глобальные парсеры, поэтому работают на всех шести языках.
2. **Кликабельные примеры** в пустом состоянии — первая задача в одно нажатие,
   фразы показывают синтаксис текущего языка.
3. **Отмена разрушительного действия.** «Очистить всё» и «Убрать выполненные»
   получают снимок состояния и кнопку «Отменить» на 9 секунд. Возврат идёт
   через тот же `window.tasks` + `save()` + `render()`, своей копии состояния
   слой не держит. `toast()` приложения сознательно не трогали — он пишет в
   `#quadHint` и участвует в самотестах.
4. **Приоритет не выдумывается.** Слова-маркеры («срочно», «urgent», «截止»)
   лишь **предлагают** нажать флажок — нажатие делает сам пользователь, логика
   квадрантов не подменяется. Выдуманных процентов уверенности нет.
5. **Клавиши** `/` (в поле ввода) и `?` («Как это работает»).

### Проверка

- `w8_unit.js` 78/78 и `w9_unit.js` 84/84 — **те же результаты, что до правок**
  (доказательство отсутствия регрессии парсеров).
- `app.html?selftest=1` → `OK 41/41`, консоль чистая.
- Новый `C:\Users\HADJAL\w11_integration.js` — **114/114** по http в headless:
  шесть языков приложения (перевод, ноль непереведённых строк, переключатель,
  разбор фразы на своём языке), пять языков лендинга, `?lang=` перекрывает
  детект, выбор переживает перезагрузку, чипы/примеры/отмена/клавиши.
- Живой прод после пуша: `superday.fun/app.html` при `en-US` → интерфейс
  английский, ноль непереведённых, ноль ошибок; `superday.fun` при `de-DE` →
  «Klarheit statt Chaos». `sw.js` отдаёт `superday-v23`.

### Честные ограничения

- Словарь по строкам **хрупок к переформулировке**: волна 12 переписала тексты
  первого экрана прямо во время работы — строки выпали и остались русскими,
  словарь пришлось синхронизировать. Ловится самотестом покрытия. Следующий
  шаг — перевести словарь на устойчивые ключи `data-i18n`.
- Для ES/DE/FR/ZH интерфейс переведён полностью, а **разбор речи базовый**
  (цифровое время, дни недели, простые обороты повтора) — это написано прямо
  в разделе «Язык» в панели, а не умолчано.
- Названия языков в списке выбора пишутся на своих языках нарочно и в счёт
  непереведённых строк не идут.

## Волна 13 — приглашения, подарки и «день по ссылке» (2026-07-26, OS 3.3)

Запрос основателя: реферальная программа и подарки за приведённого друга.
Новый внешний файл `refer.js` (подключён в `app.html` и `index.html`), `sw.js` →
`superday-v24`. Схема данных не менялась (SCHEMA = 5), логика `app.html` не
правилась — слой пользуется её же глобальными `tasks`/`save`/`render`/
`readTemplates`/`writeTemplates`/`renderTemplates`.

### Рамка честности, из которой выросла механика

Бэкенда у веб-версии нет, код открыт — **проверить «друг действительно пришёл»
невозможно**. Отсюда три решения, а не одно:

1. Дарится только то, что приложение выдаёт само и честно — **рабочие шаблоны
   дня** (они реально грузятся в маршрут кнопкой «Загрузить»).
2. Денег, подписки и «двигаем очередь» **не обещается вообще**.
3. Ограничение написано прямо в разделе: «без сервера приложение верит коду на
   слово — поэтому за друзей дарятся шаблоны и оформление».

Настоящая антифрод-проверка рефералов остаётся `needs_backend`.

### Что сделано

- **Свой код** `superday_ref_v1` (6 символов без похожих букв) и ссылка
  `superday.fun/?ref=КОД`.
- **Подарок приглашённому — сразу** при открытии по ссылке: два готовых дня
  («Утро силы», «Спокойный вечер») ложатся в его раздел «Шаблоны дня».
- **Подарок приглашающему — авансом**, по нажатию «Позвать друга»: шаблон
  «Глубокая работа». Ждать ответа системного «Поделиться» **нельзя** — веб не
  сообщает, отправил ли человек ссылку; обещанный подарок должен выдаваться,
  а не зависеть от того, что вернёт браузер. Это же и психологически вернее:
  щедрость вознаграждается сразу, а не через месяц ожидания.
- **Код благодарности** — как приглашённый подтверждает друга: код вида
  `соль(3) + отпечаток(4)` от кода приглашающего. Соль своя у каждого гостя,
  поэтому второй приглашённый даёт другой код; принятые коды запоминаются
  (`superday_friends_v1`), повтор не засчитывается. Это защита от опечатки и
  повторного ввода, **не** от подделки.
- **«День по ссылке»** — виральность без сервера: план упаковывается в
  `#plan=…` (base64url, до 12 дел), данные едут внутри адреса и никуда не
  отправляются. Друг открывает, видит карточку «Друг поделился планом дня» и
  добавляет к себе; дубликаты по тексту пропускаются, адрес после импорта
  очищается. Ловится и `hashchange` — ссылка, вставленная в уже открытую
  вкладку, тоже срабатывает (первый прогон это и поймал).
- **Лендинг** подключает слой только чтобы запомнить `?ref=` (origin общий с
  приложением) — панель приглашений там не рисуется.
- Тексты слоя — на шести языках волны 11.

### Проверка

`C:\Users\HADJAL\w13_integration.js` — **31/31** по http: свой код и ссылка;
коды благодарности (свой подходит, чужой нет, повтор и мусор отвергаются);
оба подарка и отсутствие дублей при повторном приглашении; упаковка →
распаковка → импорт плана, повторный импорт не дублирует; приход по `?ref=`,
своя ссылка приглашением не считается; лендинг только запоминает код.
Плюс самотесты приложения 41/41, `w11_integration` 114/114, `w8_unit` 78/78.
Живой прод: `superday.fun/app.html?ref=…` → приглашение запомнено, два
шаблона-подарка выданы, 0 ошибок консоли.

### Ключи хранилища волны

`superday_ref_v1` (свой код), `superday_invited_by_v1` (кто позвал),
`superday_ref_salt_v1` (своя соль как гостя), `superday_friends_v1` (принятые
коды), `superday_gift_host_v1` / `superday_gift_guest_v1` (подарки выданы).

## Волна 14 — дизайн-совет: почему одна строка, а не карточки/карта (2026-07-26, OS 3.0)

Запрос основателя (пересказан через третьи руки, поэтому разобран по частям,
а не выполнен буквально): собрать «величайшую команду психологов, дизайнеров,
архитекторов и хакеров», объяснить, почему дело в интерфейсе — одна строка, а
не карточка или карта, и где обещанные «8K-эффекты». Все три вопроса уже имеют
ответ в коде — эта волна не переизобретает решения волн 11–13, а **впервые
формулирует их одним связным обоснованием** и добавляет одну точечную правку,
не нарушающую уже найденный баланс. `sw.js` → `superday-v25`. Схема данных не
менялась (SCHEMA = 5), новых экранов и DOM-узлов не добавлено.

### Почему не карточки и не карта — пять углов одного решения

- **Психология (когнитивная нагрузка).** Закон Хика: время выбора растёт с
  числом одновременно видимых вариантов. Карточная доска или карта — это
  всегда *два* измерения выбора сразу (что делать + где на доске/карте это
  лежит), тогда как единственный маршрут (`renderPlan()`, `app.html:2092`)
  оставляет одно: «что дальше по списку». Модель когнитивной нагрузки Свеллера
  и decision fatigue (Баумайстер) — та же причина, по которой волна 11 в
  мобильном клиенте (`docs/UI_BUDGET.md`) ограничила карточку задачи двумя
  слотами: лишнее измерение выбора не бесплатно, за него платят вниманием.
  Эффект Зейгарник объясняет, почему незакрытое дело не должно «теряться» в
  свободной раскладке — оно явно переносится кнопкой «→ завтра» (`.tdef`,
  `app.html:1454`), а не остаётся висеть в неопределённом углу карты.
- **Архитектура.** `taskRow()` (`app.html:1396`) — единственная функция,
  строящая дело и для маршрута, и для матрицы; в коде прямо написано, почему:
  «раньше дело рисовалось дважды разным кодом... теперь строку строит одна
  функция». Карточная/канбан-раскладка потребовала бы второй системы координат
  (позиция на доске) поверх уже существующей (позиция в маршруте) — это не
  более гибко, а два источника правды об одном и том же порядке.
- **Дизайн (Rams, Tufte).** «Weniger, aber besser» и минимизация data-ink:
  приоритет уже передан точкой квадранта (`.qd`) и местом в списке — рамка
  карточки добавила бы контур ради контура, не новую информацию.
- **Инженерный/хакерский взгляд — минимальная поверхность.** Меньше типов
  интерактивных зон — меньше состояний, которые можно сломать. Тот же принцип,
  которым волна 12 объяснила разделение кинематики и логики («кинематика не
  знает о логике, логика не знает о кинематике», `COORDINATION.md:736-738») —
  здесь он применён к разметке: одна форма строки, а не N вариантов карточки
  под разные экраны и состояния.
- **Кино/8K.** «8K-эффекты» — уже сделанная волна 12: восемь векторных
  кинематических эффектов на composite-свойствах (`app.html:610-780`), явно не
  растровых, чтобы оставаться резкими на любом DPI. Это прямой ответ на вопрос
  «где 8K»: не в картинках, а в том, что холст искр и вся геометрия
  масштабируются по `devicePixelRatio`.

### Что добавлено (эффект 3 продолжен, не создан заново)

«Дыхание приоритета» раньше расцветало только у точки квадранта первой
строки. Волна 14 синхронизирует с этим же моментом мягкую тень у самой
строки — `@keyframes rowLift` рядом с `dotBloom` (`app.html:633-645`):
иерархия «начни отсюда» читается ещё и глубиной, а не только цветом точки —
и по-прежнему без рамки карточки, потому что рамки в этом интерфейсе
принципиально нет. Правки — только внутри существующего CSS-блока
«кинематика сборки дня», ни одного нового узла DOM.

Отдельно проверена гипотеза, что каскад (`fxPlanBuilt`) может не подхватывать
пересборку маршрута после «Разгрузить день» (`showUnloadPlan` →
`app.html:2181`) — **гипотеза не подтвердилась**: кнопка «Перенести на
завтра» вызывает `save(); render();`, а `render()` уже сам вызывает
`renderPlan()` → `fxPlanBuilt(ol)` (`app.html:1541`), то есть каскад и так
играет на этом пути. Правило волны 6 из `docs/UI_BUDGET.md` («без служебных
цифр — если нечего сделать с фактом, его не показывают») распространяем и на
сам этот документ: не чинить то, что не сломано.

### Проверено

Штатный MCP-браузер оказался занят другим процессом (общий профиль,
`EBUSY` на lock-файле — не трогаем чужую сессию), поэтому проверка велась
через отдельный headless Chromium (Playwright, изолированный профиль) поверх
локального `python -m http.server` в каталоге репозитория:

- `app.html?selftest=1` — `document.title` = «OK 41/41 — SUPER DAY selftest»
  в обоих запусках (обычный режим и `reducedMotion: reduce`), состав тестов
  не менялся с волны 12/13.
- Консоль (`console` + `pageerror`) — в обоих режимах чисто, ошибок нет.
- `.rowin` в этом прогоне на пустом состоянии (чистый профиль без сохранённых
  дел) не встречается ни в одном из двух режимов — значит эмпирически
  сравнить проигрывание `rowLift` не на чем. Логически поведение гарантировано
  тем же механизмом, что и у `dotBloom`: `rowLift` висит на том же селекторе
  `ol.plan li.rowin:first-child`, а класс `.rowin` добавляет исключительно
  `fxPlanBuilt()` и только когда сокращение движения выключено — тот же
  переключатель, что уже подтверждён волной 12.
- Новых ключей `localStorage` нет.

## Волна 15 — доступность аффордансов: ручка приоритета, точка квадранта, подпись длительности (2026-07-26, OS 3.3)

Формулировка задачи была грубой и требовала уточнения, поэтому вопрос задан
пользователю напрямую и получен явный ответ на два пункта: (1) это не про
тон текста (волна 12 уже сознательно убрала «детскость» — решение
окончательное, не трогается) — это про **аффордансы**: подписи к
действиям, объяснение результата, устранение скрытых жестов; (2) это не
только веб, но параллельно и мобильный клиент (см. отдельный трек ниже).
Дальше формулировка переведена на язык, которым пользуется дисциплина
доступности интерфейсов, а не повторяет исходную фразу буквально —
устойчивость к когнитивной нагрузке нужна не «особому» пользователю, а
любому: уставшему, спешащему, человеку без мыши, человеку с экранным
диктором. Это ровно предмет WCAG 2.2 Cognitive Accessibility Guidance
(COGA), affordance/signifier-разбора Дональда Нормана и «Не заставляй меня
думать» Стива Круга — обоснование ниже строится на них, а не на
хлёсткости исходного запроса.

### Что найдено аудитом (реальный код, не гипотеза)

Проверка прошла по каждому интерактивному узлу `taskRow()` (`app.html:1406`).
Большинство уже accessible: `.chk` — `role="checkbox"`, `tabIndex`,
`aria-checked`, клавиатура (Space/Enter); `.ttime`, `.trep`, `.tdef` — кнопки
с явным текстом и `aria-label`. Один узел оказался настоящим разрывом, два —
мелкими шероховатостями:

1. **`.grip` (ручка «перетащи в другой квадрант», `app.html:1496` было) —
   критично.** `<span aria-hidden="true">` с единственным обработчиком
   `pointerdown` и `title`, видимым только по hover. По Норману это
   affordance без signifier: возможность действия существует (мышь/палец
   тянут строку между квадрантами), но не объявлена — ни клавиатуре, ни
   экранному диктору, ни тачу без hover. Единственный способ сменить
   приоритет дела в матрице был недоступен целиком без мыши.
2. **`.qd` (точка приоритета в маршруте, `app.html:1481`) — незначительно.**
   Смысл цвета уже раскрывается по клику через `spotTask()` (открывает
   панель матрицы и подсвечивает дело) — прогрессивное раскрытие уже было в
   коде. Разрыв — только *до* клика, на тач-экранах без hover нет мгновенной
   текстовой подсказки рядом с местом нажатия.
3. **`.tdur` в пустом состоянии (`app.html:1446`) — незначительно.** Видимый
   текст «＋длит.» — сокращение, полное слово было только в `title`/
   `aria-label`. Круг: «не заставляй меня думать» — расшифровывать
   сокращение не должно быть задачей пользователя.

### Правки (только доступность, макет и язык волны 12 не тронуты)

- **`.grip` теперь фокусируется и управляется с клавиатуры.**
  `aria-hidden` убран, добавлены `tabIndex=0`, `role="button"`, `aria-label`
  с текущим квадрантом дела и подсказкой «стрелка вверх/вниз — сменить
  квадрант» (`app.html:1496`, taskRow). Логика смены квадранта, которая
  раньше жила только внутри мышиного `onUp()` (`app.html:2440`), вынесена в
  общую функцию `assignQuad(t, qi)` рядом с `QSET` (`app.html:2405`) — и
  drop мышью, и стрелки с клавиатуры теперь дёргают одну и ту же точку
  правды, а не два места, которые могут разъехаться. После смены квадранта
  дело объявляется через уже существующий `toast()` (переиспользован, не
  создан заново) и фокус явно возвращается на `.grip` той же строки после
  `render()` — иначе клавиатурный пользователь на каждом шаге «падал» бы в
  начало списка, известная ловушка при живой пересборке DOM.
- **`quadHint` (место, куда пишет `toast()`) получил `aria-live="polite"` +
  `role="status"`** (`app.html:747`) — раньше все тосты приложения (время,
  длительность, повтор, перенос, смена квадранта) были невидимы для
  экранного диктора; это одна атрибутная правка чинит доступность сразу
  всех существующих мест, использующих `toast()`, не только новой ручки.
- **`.qd` теперь эхо-объявляет смысл точки перед открытием панели** —
  `toast(QUAD_SHORT[qi]+" — "+QUAD_NAME[qi])` добавлен перед `spotTask()`
  (`app.html:1486`). Не новый DOM-узел и не новая механика — переиспользован
  тот же `toast()`, что и везде в приложении; частоту не ограничивали
  намеренно (правило 6 UI_BUDGET про служебные цифры сюда не относится, а
  сам toast и так не заменяет панель — только даёт мгновенное эхо у места
  тапа).
- **`.tdur` пустое состояние: «＋длит.» → «＋длительность»** (`app.html:1446`).
  Ширина блока — `flex:0 0 auto` по контенту, не фиксированная колонка,
  поэтому раскладка `.tmeta` не ломается; проверено скриншотом Playwright на
  360px (см. «Проверено»).
- CSS: `.grip:focus-visible` добавлен в общий селектор `outline-offset`
  (`app.html:254`) — по тому же паттерну, что уже есть у `.chk`/`.tg`;
  видимого фокуса без этого не было бы даже с клавиатурной поддержкой.
- `sw.js` → `superday-v26`.

### Что осознанно не тронуто

Тон и лексика текста (волна 12 — финальное решение), карточная/канбан-
раскладка (волна 11/14 — не вводится), `SCHEMA` (осталась 5), восемь
кинематических эффектов волны 12 (не изменялись), частота показа toast для
`.qd` (решено не ограничивать, см. выше).

### Мобильный трек (параллельно, `optimizator-dnya-mobile`)

Тот же класс проблемы — иконка-состояние без объявленного альтернативного
канала — найден в `src/components/TaskCheckbox.tsx`: `Pressable` без
`accessibilityRole="checkbox"`, `accessibilityState`, `accessibilityLabel`.
Это не новая механика и не повод заводить девятый пункт `UI_BUDGET.md` — это
частный случай уже существующего **правила 8** («область нажатия равна
обещанию»): обещание должно считываться любым каналом восприятия, которым
пользуется человек в моменте, включая экранный диктор, а не только
визуально/тактильно. Правка и запись в «Где правило уже применено» —
отдельным треком в репозитории мобильного клиента.

### Проверено

Тот же обходной путь, что и в волне 14 — общий профиль MCP Playwright занят
другим процессом, поэтому проверка через изолированный headless Chromium
поверх локального статического сервера в каталоге репозитория:

- `app.html?selftest=1` — «41/41» в `document.title`, в обычном режиме и с
  `reducedMotion: reduce`; консоль в обоих прогонах чиста.
- Клавиатура: `.grip` — программный `focus()` подтверждён через
  `document.activeElement`, `aria-hidden` отсутствует, `aria-label`
  содержит текущий квадрант; `ArrowDown` меняет `urgent/important` у дела и
  пишет текст в `quadHint`; после смены фокус остаётся на `.grip`; четыре
  нажатия `ArrowDown` подряд возвращают исходный квадрант (цикл замкнут).
- `.tdur` на ширине 360px: расшифрованный текст не выходит за пределы
  `.tmeta` (сравнение bounding box).

## Волна 16 — доступность аффордансов, часть 2 (2026-07-26, OS 3.3)

Волна 15 закрыла не всю тему — целевой повторный аудит (тот же метод: искать
узлы, чьё действие или состояние сигнализируется только визуально/тактильно)
нашёл дальнейшие экземпляры того же класса разрыва между affordance и
signifier. Правки ниже — продолжение волны 15, не новая тема.

### Что найдено аудитом

1. **`.txt` (текст задачи/заметки, `taskRow()` и `renderNotes()`) —
   критично.** Единственный способ переименовать дело или заметку —
   `<span class="txt">` только с `click`-обработчиком и `title`, видимым по
   hover; `cursor:text` в CSS сигналит «читай», а не «нажми». Полностью
   недоступно с клавиатуры и для экранного диктора — более серьёзный разрыв,
   чем закрытая в волне 15 `.grip`, потому что задевает базовое действие
   (переименование), а не второстепенное (смена приоритета).
2. **Кнопка микрофона (`#micBtn`), режим диктовки — незначительно.**
   Состояние записи передавалось только цветом и CSS-анимацией `pulse`
   (`.mic-btn.rec`), без `aria-pressed` — в отличие от соседнего `vmToggle`
   (голос без рук), который такое состояние уже объявляет.
3. **Причина отключения микрофона без HTTPS — незначительно.** `aria-label`
   выставлен один раз при разметке и не обновлялся, когда кнопка становится
   `aria-disabled`; объяснение («Голос доступен только по HTTPS») было видно
   только в `title`.
4. **Эхо переключения трафика (`cycleTraffic()`) — низкий приоритет.**
   `#dayVerdict` уже `aria-live="polite"`, то есть не немой узел, но
   `innerHTML` перестраивается синхронно в один тик — паттерн, который часть
   экранных дикторов объявляет не всегда надёжно. `.qd` в волне 15 такой же
   риск закрыла явным эхо через `toast()` — трафик его не получил.
5. **`icsBtn`/`dv-chip` — мелкое.** `title` части кнопок нёс больше смысла,
   чем видимый текст, а `aria-label` не зеркалировал его.

### Правки

- **`makeTextEditable(tx, text)`** — новый общий помощник рядом с
  `inlineEdit()` (`app.html`): `role="button"`, `tabIndex=0`, `aria-label`
  с текущим текстом и подсказкой «нажми Enter, чтобы изменить», `keydown`
  на Enter/Space вызывает `tx.click()` — использует уже существующий
  `click`-обработчик `inlineEdit(...)`, а не дублирует логику. Применён в
  `taskRow()` и `renderNotes()`, `.txt:focus-visible` добавлен в общий
  селектор `outline-offset` тем же приёмом, что и `.grip`/`.chk`.
- **`#micBtn` получил `aria-pressed`**, переключается вместе с
  `classList.add/remove("rec")` во всех трёх местах, где это происходит:
  клик (диктовка вкл/выкл), `rec.onend`, `rec.onerror`.
- **`#micBtn`/`#vmToggle` в HTTPS-заблокированном состоянии получили
  `aria-describedby="vmStatus"`** — ссылка на уже существующий текст
  объяснения, новая строка не придумывалась.
- **`cycleTraffic()` объявляет новое значение через `toast()`** — тот же
  переиспользуемый вызов, что и у `.qd` в волне 15, не новый DOM-узел.
- **`icsBtn` и три `.dv-chip` (`renderVerdict()`) получили `aria-label`,
  зеркалирующий `title`** — без изменения видимого текста.
- `sw.js` → `superday-v27`.

### Что осознанно не тронуто

Свайп с края экрана для открытия панели (`app.html:3654-3669`) — жест без
клавиатурной альтернативы, но альтернатива есть (`#panelBtn`, `Esc`), поэтому
не тема волны про недостающие сигналы; раскладка панели и тон текста — как и
в волне 15, не трогаются.

### Мобильный трек (параллельно, `optimizator-dnya-mobile`)

Тот же повторный аудит на мобильном клиенте нашёл семь дальнейших мест под
правило 8 `UI_BUDGET.md` (карточка задачи, несколько тумблеров-раскрытий,
выбор чипов, немой момент триумфа) — фиксируется отдельным треком в
репозитории мобильного клиента как его собственная Волна 15.

### Проверено

Тот же обходной путь, что и в волнах 14–15 — изолированный headless Chromium
поверх локального статического сервера в каталоге репозитория:

- `app.html?selftest=1` — «41/41» в `document.title`, консоль чиста.
- Клавиатура: `Enter`/`Space` на `.txt` открывает `inlineEdit` (проверено и
  для строки задачи, и для заметки); `aria-pressed` у `#micBtn` переключается
  true/false вокруг `rec.start()`/`rec.stop()`; `toast()` вызывается при
  клике по `.dv-chip` трафика.
- Баланс `{}` в `<style>`, `new Function` по инлайн-JS не встречается.

## Волна 17 — доступность аффордансов, часть 3: визуальный signifier (2026-07-26, OS 3.3)

Продолжение волн 15–16 тем же методом (искать узлы, чьё действие сигнализируется
не всеми каналами разом), но с сужением скоупа по прямому запросу основателя —
именно **визуальная очевидность**: hover/cursor/тень на кликабельных элементах,
явные состояния «нажато»/«отключено», видимые границы аккордеонов и свайп-зон.
Целевой повторный аудит по этому единственному критерию нашёл ровно один
оставшийся разрыв.

### Что найдено

**`.txt` (текст задачи/заметки) — единственная находка.** Волна 16 закрыла
клавиатурный и скринридерный канал (`role="button"`, `tabIndex`, `aria-label`
в `makeTextEditable()`), но визуальный сигнал остался старым: CSS-правило
`cursor:text` не менялось с тех пор, как элемент правился только по клику
мышью, — курсор говорит «здесь можно выделить текст», а не «здесь можно
нажать», и никакого hover-отклика не было вовсе (только `title`, видимый
не сразу). После волны 16 разрыв стал внутренне противоречивым: клавиатура
и диктор уже знают, что это кнопка, а взгляд — ещё нет.

### Правка

- `li.task .txt, ul.notelist li .txt` — `cursor:text` → `cursor:pointer`.
- Добавлено `:hover` — смена цвета на `var(--champagne)` и подчёркивание
  (`text-decoration-color:var(--hair-soft)`), тем же приёмом, что уже
  используют `.grip:hover`/`.pg-head:hover` в этом же файле — новый визуальный
  язык не вводился, переиспользован существующий.
- Комментарий над `makeTextEditable()` обновлён, чтобы не вводить в
  заблуждение упоминанием `cursor:text` как обоснования доступности.
- `sw.js` → `superday-v28`.

### Что осознанно не тронуто

Остальные пункты аудита волны 16 (`#micBtn` `aria-pressed`, `aria-describedby`,
эхо `cycleTraffic()`, `aria-label` у `icsBtn`/`.dv-chip`) — уже визуально
корректны независимо от screen-reader слоя (цвет/анимация/`title` были и
остаются видимы глазом), поэтому вне сужённого скоупа этой волны. Аккордеоны
панели (`.pgroup`) и свайп-зона панели визуальных границ не потеряли — не
трогались.

### Проверено

`app.html?selftest=1` через `w11_selftest.js` (тот же изолированный headless
Chromium поверх локального статического сервера) — «OK 41/41» в
`document.title`, консоль чистая.

## Волна 18 — «Карта дел»: места + путь на карте (2026-07-26, OS 3.3)

Пользователь ждал именно карту с маршрутом между местами дел, прямо в вебе —
это была основная просьба, оставшаяся не закрытой предыдущими волнами (16 и
17, см. выше, сделаны независимым инстансом и посвящены другому — доступности
аффордансов; с этой волной не пересекаются). Файлы: только `app.html`
(единственный источник фичи), `sw.js` → `superday-v29`, `SCHEMA` 5 → 6.

Перед проектированием собран «совет» из четырёх ролей (психолог ясности
интерфейса, премиум-дизайнер, инфодизайнер, красная команда) — разобрали
реальный код проекта, а не абстрактную задачу. Их выводы — ниже, как принятые
решения.

### Что сделано

- **Схема**: новое поле задачи `loc: {lat, lon, label, addr} | null`.
  Миграция — по образцу уже существующей нормализации соседних полей; логика
  вынесена в отдельную чистую функцию `normLoc(loc)` (по прецеденту
  `normRepeat(r)`), что сделало её тестируемой напрямую, без обращения к
  `migrate()`/глобальному `tasks`.
- **Ввод места**: в `.tmeta` строки задачи — бейдж `＋место` / `📍 Офис`,
  последний по порядку (наименее обязательный атрибут, тот же язык, что
  `＋время`/`＋длительность`). Клик → `prompt` с адресом, геокод через
  Nominatim **по явному действию**, не на каждый ввод. Кэш геокода в
  `localStorage` (`superday_geocache_v1`) по нормализованному тексту адреса —
  бережём fair-use лимиты сервиса.
- **Обязательное подтверждение находки**: после геокода — `confirm()` с
  текстом найденного адреса, отказ не сохраняет ничего и предлагает ввести
  точнее. Гео-точка — черновик, не факт по умолчанию; это тот же принцип
  честности данных, что уже применён к погоде в приложении.
- **Секция «Карта дел»** в панели (`data-sec="map"`, зарегистрирована в
  `PANEL_SECS`) — счётчик в `.pg-note`, дисклозура приватности (адреса —
  единственные данные, покидающие браузер), кнопка «Показать путь по N
  местам» (видна только при ≥2 точек), пустое состояние с пунктирными
  призрачными маркерами при 0-1 точке.
- **Путь**: порядок точек строго по времени задачи (тот же порядок, что
  `planOrder`) — без «оптимизации объезда» и без второй системы истины о
  порядке дня; это осознанно не отменяет решение волны 14 против
  карточной/board-раскладки списка дел — секция карты изолирована, свёрнута
  по умолчанию, не касается `renderPlan`/`planOrder`. Маршрут через публичный
  OSRM, один запрос на весь набор точек. Честный, визуально отличимый
  fallback при недоступности OSRM — пунктирная прямая линия с явной подписью
  «по прямой, без учёта дорог» и **без** оценки времени в пути (у прямой
  линии нет модели скорости — показывать цифру было бы нечестно).
- **Оформление**: тайлы CARTO Dark Matter без подписей + CSS-фильтр и
  тонирующий overlay в тон приложения; CSS Leaflet инлайнен в общий
  `<style>` (внешний `<link>` заблокировал бы `style-src` CSP) — сам Leaflet
  JS и подключение карты грузятся лениво, только при раскрытии секции, а не
  при загрузке приложения. Маркеры — `L.divIcon`, кружок в цвете квадранта +
  номер, тот же мотив, что `ol.plan li::before`. Клик по строке ↔ маркеру —
  через существующий механизм `spotTask`.

### Самотесты

Новая группа «Карта дел» (6 проверок, добавлены в `runSelfTests`) —
`normLoc` (валидное место, битое/отсутствующее → `null`), `geoCacheKey`
(регистр/пробелы не влияют на ключ, разные адреса — разные ключи),
`haversineKm` (расстояние точки до себя = 0, Москва—Петербург ≈ 600–670 км).
Никаких реальных сетевых вызовов — геокод и OSRM-роутинг из самотестов
исключены сознательно (недетерминированность + fair-use лимиты сервисов).

### Проверено

`new Function(...)` на извлечённом основном инлайн-скрипте — синтаксис OK
(157099 симв.); баланс `{`/`}` в CSS — 490/490; новая группа самотестов
«Карта дел» встроена в общий набор `?selftest=1` без сетевых вызовов.

## Волна 19 — «Намерение дня» + итог с аффирмацией (2026-07-26/27, OS 3.3)

Закрытие разрыва паритета с мобайлом: у мобильного клиента утренний ритуал
выбора идентичности («намерение дня», по Гольвитцеру — implementation
intentions) и вечерний итог с ним существовали давно (`dayIntention.ts`,
`daySummary.ts`, `DaySummaryModal.tsx`), у веба — не было вовсе. По прямой
директиве пользователя «все изменения должны быть и в вебе и в мобилке»
([[feedback_cross_platform_parity]]) это одновременно и следующий пункт
веб-бэклога («2) утренний/вечерний ритуалы»), и парная работа. Файл: только
`app.html`, `sw.js` → `superday-v30`. `SCHEMA` не менялась (данные намерения
не относятся к задачам, отдельный ключ).

### Что сделано

- **Утренний бар «Намерение дня»** (`#intentionBar`, дублирует CSS-класс
  `.rollover` существующего бара переноса дел — та же визуальная роль,
  «мягкое предложение сверху», а не новый компонент/модалка). Шесть готовых
  фраз-идентичностей (`INTENTION_PRESETS`, дословно те же, что в мобильном
  `dayIntention.ts` — терминологическое зеркалирование) + необязательное
  свободное поле (до 120 символов). Выбор — одиночный, `role="radiogroup"` +
  `role="radio"`/`aria-checked` на чипах, а не просто цвет — тот же паттерн
  accessibility, что `IntentionModal.tsx` получил на мобайле в волне 15
  (`docs/UI_BUDGET.md`, правило 8). Кнопка «Не сегодня» — отказ на день без
  завтрашнего повтора вопроса (ключ `superday_intention_skip`).
- **Хранение**: `superday_intention_v1`, день-скоуп через `todayKey()` —
  тот же паттерн, что `DAYCTX_KEY`/`ONBOARD_KEY`. Намерение просится один
  раз в день; уже заданное — показывается баром-эхо с кнопкой «Изменить».
- **Вечерний итог** — `renderVerdict()` расширен: между блоком контекста
  (`.dv-ctx`) и фразой-вердиктом вставлен `.dv-intent` — «Намерение дня:
  «фраза»[ — заметка]» (напоминание того, что было выбрано утром, не
  повторный выбор). Когда день закрыт полностью (`v.total>0 &&
  v.done===v.total`) и намерение было задано — под фразой добавляется
  `.dv-affirm`, закрывающая строка по правилу пика-конца (Peak-End rule),
  ротируемая детерминированно по числу закрытых дел (`dayAffirmation(n)` =
  `AFFIRMATIONS[n % AFFIRMATIONS.length]`), **не** `Math.random()` — тот же
  день при повторном открытии должен показывать ту же фразу.
- **Безопасный DOM**: фраза намерения и заметка — пользовательский текст,
  вставлены через `createElement`/`createTextNode`/`appendChild`, как уже
  делает соседний код `.dv-ctx` в той же функции — не `innerHTML`, XSS
  исключён архитектурно, а не проверкой.
- **Осознанно не перенесено** с мобайла: XP/streak-механика намерения —
  платформо-специфична (мобильный геймификационный слой), согласно явному
  исключению в [[feedback_cross_platform_parity]].

### Самотесты

Новая группа «Намерение дня» (4 проверки) — `dayAffirmation` (ротация по
модулю, включая переход через границу массива), `loadIntention` (нет записи
→ `null`, запись за чужой день → `null`), `saveIntention`/`loadIntention`
(сегодняшняя запись читается обратно, `note` триммится, снимает отметку
«не сегодня»), `skipIntentionToday`/`intentionSkippedToday` (round-trip).
Каждый тест сохраняет и восстанавливает предыдущее значение соответствующих
ключей `localStorage`, чтобы не задеть реальные данные пользователя при
прогоне `?selftest=1` на проде. Итог: `?selftest=1` — `OK 52/52`.

### Проверено

`new Function(...)` на извлечённом инлайн-скрипте — синтаксис OK; баланс
`{`/`}` в CSS — сведён; headless Chrome (`--headless=new --disable-gpu
--no-sandbox --dump-dom`) на `app.html?selftest=1` — `OK 52/52` в
`document.title`, в stderr нет `Refused to`/CSP/`Uncaught`/`ReferenceError`/
`TypeError`/`SyntaxError`.

## Волна 20 — премиальная карта маршрута на главном экране (2026-07-27, OS 3.3)

Прямой запрос основателя после демонстрации волны 18: карта с делами должна
быть заметным премиальным элементом **на главном экране**, а не опцией,
спрятанной в правой панели — и одновременно нужно снизить плотность
информации на главном экране, перенеся статический поясняющий текст в уже
существующую панель. Изначально этот план был написан как «волна 19», но
пока он ждал одобрения, вторым агентом параллельно была закрыта и
закоммичена собственная волна 19 («Намерение дня») — поэтому здесь и в имени
плана `twinkling-puzzling-comet.md` сохранён старый номер, а в журнале
запись идёт под следующим свободным, 20-м. Файл: только `app.html`, `sw.js`
→ `superday-v31`. `SCHEMA` не менялась.

### Что сделано

- **Карта переехала в `.route`**: карточка `#mapCard`/`#mapView`/`#mapNote`
  теперь стоит прямо под `<h3>Сегодня</h3>`, перед `#daySummary` и списком
  дел — вместо прежнего расположения внутри свёрнутой панели за клик по
  `#mapGoBtn`.
- **Автопоказ вместо клика**: `renderMapCard()` переписана — сама решает,
  показывать ли карту, по числу дел с местом (`tasksForMap().length`). При
  <2 мест карточка скрыта и главный экран выглядит ровно как раньше, без
  плейсхолдера; при ≥2 — карточка открывается и лениво подгружает Leaflet
  (`loadLeaflet()`) с фолбэком-подсказкой при сетевой ошибке. Прежний
  `openMap()` и клик-обработчик `#mapGoBtn` удалены — нажимать больше
  нечего. `tasksForMap()`, `buildMapView()`, `loadLeaflet()`, `mapPopup()`,
  `mapState`, `QCOLOR` волны 18 переиспользованы без изменений.
- **Панель разгружена**: раздел «Карта дел» (`#mapEmpty`/`.me-dots`/
  `#mapGoBtn` и сама секция `data-sec="map"`) убран из панели целиком вместе
  с записью в `PANEL_SECS` и вызовом `note("map", …)` — карта живёт только
  на главном экране, дублирующей точки входа в панели больше нет.
- **Разгрузка главного экрана**: единственный статический поясняющий абзац
  `.rsub` («порядок маршрута: сначала то, что горит…») перенесён из `.route`
  в раздел панели «Как это работает» (дополнен фразой про появление карты);
  `#daySummary`/`#dayVerdict` — ежедневно новый контент — на месте без
  изменений, как и было решено заранее.
- **Волна 14 не отменяется**: `planOrder()` остаётся единственным
  источником порядка дня (`COORDINATION.md:995`) — карта ничего не
  переупорядочивает, только визуализирует уже посчитанный порядок на новом,
  более заметном месте экрана.

### Самотесты

В группу «Карта дел» добавлены 2 теста на скрытую ветку `renderMapCard()` —
карточка скрыта при 0 местах и при 1 месте (нужно ≥2); ветка с ≥2 местами
намеренно не тестируется — она дёргает `loadLeaflet()`/CDN, а самотесты не
должны трогать сеть (то же правило, что уже в волне 18). Итог:
`?selftest=1` — `OK 54/54`.

### Проверено

`new Function(...)` на извлечённом инлайн-скрипте — синтаксис OK; баланс
`{`/`}` в CSS — 491/491, сведён; headless Chrome (`--headless=new
--disable-gpu --virtual-time-budget=5000 --dump-dom`) на
`app.html?selftest=1` — `OK 54/54` в `document.title`. Полнотекстовый grep
по `mapEmpty|mapGoBtn|me-dots|map-go\b` по всему файлу — 0 совпадений,
висящих `getElementById` на удалённые id не осталось.

## Волна 21 — импорт `.ics` обратно (2026-07-27, OS 3.3)

Следующий пункт бэклога по убыванию ценности (кандидат №2): волна 9 сделала
мост «план дня → системный календарь» только в одну сторону; эта волна
замыкает его — встречи из рабочего календаря (Outlook/Google, экспортированные
файлом `.ics`) попадают в план дня SUPER DAY. Файл: только `app.html`, `sw.js`
→ `superday-v32`. `SCHEMA` не менялась.

### Что сделано

- **Разбор `.ics` — набор чистых функций**, симметричный существующему
  `buildICS()`/`icsEsc()`/`icsFold()`: `icsUnfold()` (обратная операция
  `icsFold` — разворачивает folded-строки RFC 5545 §3.1, продолжение начинается
  с пробела/таба), `icsUnesc()` (обратная `icsEsc` — снимает экранирование
  запятой/точки-с-запятой/бэкслеша/`\n`), `icsParseDT()` (разбирает
  `YYYYMMDD[THHMMSS[Z]]` и `VALUE=DATE` во `{key, time}`), `parseICS()`
  (вытаскивает `VEVENT`-блоки: `UID`/`SUMMARY`/`DTSTART`/`DTEND`).
- **`icsEventsToTasks(events, seenUids, todayStr)`** — чистая функция без
  побочных эффектов (не трогает `tasks`/`localStorage`), тестируемая
  напрямую: фильтрует события в прошлом (`dtstart.key < todayStr`),
  дедуплицирует по `UID`, считает длительность из `DTSTART`/`DTEND` (только
  если оба со временем и `DTEND` позже), выставляет `defer` — `null` для
  сегодняшних, дату — для будущих (та же пара `todayKey()`/`defer`, что и в
  ручном переносе).
- **Сознательный отказ от разворачивания `RRULE`**: регулярность из чужого
  календаря не пытаемся угадывать по неполным данным стороннего формата —
  одна встреча `.ics` = одна разовая задача. Честнее перенести встречу один
  раз, чем молча придумать серию.
- **Идемпотентность через `UID`**: список уже перенесённых `UID` живёт в
  `superday_ics_imported_v1` — повторный импорт того же файла (или
  обновлённого календаря с теми же встречами) не задваивает задачи, только
  добавляет новые.
- **`importICS(text)`** — оркестрация поверх чистых функций: парсит,
  фильтрует/дедуплицирует, пушит задачи тем же паттерном, что и
  JSON-восстановление (`uid()` на каждую, `save()`+`render()` один раз в
  конце), тост с разбивкой «добавлено / уже было / пропущено прошедших».
- **UI**: кнопка «📥 Встречи из .ics» + скрытый `<input type="file">` в
  разделе «Данные и календарь» (`data-sec="data"`), рядом с парой
  экспорт/импорт JSON-резервной копии — тот же визуальный и структурный
  паттерн (`click()` по кнопке → `change` → `FileReader.readAsText`).

### Самотесты

Новая группа «Импорт .ics» (6 проверок): `icsUnfold` разворачивает
продолжение строки; `icsParseDT` — время и весь день; `parseICS` — `SUMMARY`/
`UID`/`DTSTART`/`DTEND` и снятие экранирования запятой; `icsEventsToTasks` —
длительность из интервала + дедуп по `UID` + отсечение прошлого одним
тестом; отдельно — уже импортированный `UID` не задваивается; отдельно —
сегодняшняя встреча без переноса, будущая — с переносом на свою дату. Итог:
`?selftest=1` — `OK 60/60` (было 54/54).

### Проверено

`new Function(...)` на извлечённом инлайн-скрипте — синтаксис OK; баланс
`{`/`}` в CSS — 491/491 (не менялся, правка не трогала стили); headless
Chrome (`node w11_selftest.js`) на `app.html?selftest=1` — заголовок
`OK 60/60`, консоль чистая (нет `Refused to`/CSP/`Uncaught`/
`ReferenceError`/`TypeError`/`SyntaxError`). `git diff --stat` до коммита —
только `app.html`+`sw.js`, без посторонних файлов.

## Волна 22 — контексты жизни (работа/дом/здоровье) + баланс недели (2026-07-27, OS 3.3)

Следующий пункт бэклога по убыванию ценности (кандидат №3): волна 10/19
отвечают на «что успел сделать» (матрица важности, серия дней, намерение
дня), но не на «на что уходит жизнь». Эта волна добавляет третье измерение —
сферу жизни дела — и агрегирует её за неделю. Файл: только `app.html`,
`sw.js` → `superday-v33`. `SCHEMA` 6→7.

### Что сделано

- **Поле `task.context`** (`"work"|"home"|"health"|null`) + `normContext(c)` —
  строгий фильтр по фиксированному списку `CTX_LIST`, любое чужеродное
  значение (в т.ч. из старого экспорта/JSON-восстановления) схлопывается в
  `null`. Миграция v7 в `migrate()` по тому же паттерну, что `normLoc`/
  `normRepeat` волны 8/18.
- **Кнопка `.tctx` в строке дела** — не текстовый ввод (как `editTaskRepeat`/
  `editTaskLoc` через `prompt()`), а цикл по кругу кликом: без сферы → 💼
  работа → 🏠 дом → ❤️ здоровье → без сферы (`nextContext`/
  `cycleTaskContext`), тем же приёмом, что смена квадранта стрелками на
  `.grip` (волна 10, a11y-проход) — для фиксированного короткого списка
  состояний цикл кликом надёжнее свободного текста: невозможно ввести
  невалидное значение, не нужен парсер синонимов.
- **`weekContextBalance(list, todayStr)`** — чистая функция без побочных
  эффектов (сигнатура нарочно такая же, как у `icsEventsToTasks` волны 21):
  берёт `done`-дела с `doneOn` в последних 7 календарных днях от `todayStr`,
  группирует по `context`, отдельно считает `untagged` (без сферы) и
  `total`. Не трогает `readStats()`/`renderStats()` (число дел в день) —
  это ортогональный срез тех же данных, а не замена.
- **UI** — блок «Баланс недели» под существующим `#statsFoot` в панели
  «Неделя» (`data-sec="stats"`): горизонтальная сегментная полоса
  (`flex-grow` по количеству, не по `%`, поэтому пересчитывать проценты не
  нужно) + подпись по каждой сфере с количеством. Пустое состояние —
  подсказка «отметь сферу дела». Новая панель в `PANEL_SECS` **не**
  заводилась: раздел «Неделя» — уже готовое место для недельных срезов,
  заводить десятую панель ради одного блока было бы лишней сущностью.

### Самотесты

Новая группа «Контексты жизни» (4 проверки): `normContext` отсекает
чужеродные значения; `nextContext` — полный цикл по всем четырём
состояниям; `weekContextBalance` — группировка по `context` с `untagged` и
`total` на смешанном наборе дел; отдельно — дело старше 7 дней не считается.
Итог: `?selftest=1` — `OK 64/64` (было 60/60).

### Проверено

`new Function(...)` на извлечённом инлайн-скрипте — синтаксис OK; баланс
`{`/`}` в CSS — 505/505 (было 491/491, +14 от нового `.tctx`/`.ctxbal`
блока — прирост соответствует объёму правки); headless Chrome
(`node w11_selftest.js`) — заголовок `OK 64/64`, консоль чистая.
`git diff --stat` до коммита — только `app.html`+`sw.js`.

Мобильный паритет — по [[feedback_cross_platform_parity]] эта фича (в
отличие от импорта `.ics` в волне 21) отвечает на пользовательский вопрос,
общий для обеих платформ («на что уходит жизнь»), а не на десктопную
файловую механику — паритет нужен, реализация следующим шагом в
`optimizator-dnya-mobile`.

## Волна 22 — мобильный паритет: сферы жизни + баланс недели (2026-07-27, OS 3.3)

Реализация анонсированного выше паритета в `optimizator-dnya-mobile`.
Логика зеркалит веб один в один (`CTX_LIST`/`normContext`/`nextContext`/
`weekContextBalance` → `computeContextBalance`), но экранная подача другая —
UI мобилки под собственным бюджетом (`docs/UI_BUDGET.md`, волна 11:
«хватит добавлять блоки — убирай»), поэтому там, где веб завёл отдельный
блок «Баланс недели», мобилка встроила ту же мысль в уже существующую
очередь одной строки.

### Что сделано

- **Поле `context?: "work"|"home"|"health"|null`** на `TaskWithLocation`
  (`src/types/index.ts`) — клиентское, как и остальные расширения задачи
  (`completed`/`bonusXp`/`planDay`...); бэкенд не хранит.
- **`src/services/context.ts`** (новый файл) — `CTX_LIST`/`CTX_LABEL`/
  `CTX_ICON`/`normContext`/`nextContext` дословно повторяют веб-логику;
  `computeContextBalance(tasks, todayKey)` — та же семантика, что
  `weekContextBalance`, но на полях мобилки (`completed`+`completedDay`
  вместо `done`+`doneOn`) и с DST-safe арифметикой дат через `Date.UTC`
  (тот же приём, что `shiftDayKey` в `services/weeklyRhythm.ts`, а не
  копия кода веба — код мобилки уже решал эту задачу для ритма недели).
- **Тап-цикл сферы в карточке задачи** — `taskCard.ts` получил
  `CardDetailKind: "context"` (по образцу уже интерактивного `"address"`,
  вставлен перед ним — комментарий «адрес последним» в файле сохранён
  дословно); `TaskListScreen.tsx` рендерит его как `Pressable`, вызывающий
  `updateTask(task.id, { context: nextContext(task.context) })` напрямую —
  новой плитики в `TasksContext` не понадобилось: `updateTask` уже
  принимает произвольный `Partial`-патч (используется так же для
  `location` в этом же файле).
- **Баланс недели — НЕ новая карточка.** На веб это отдельный блок с
  сегментной полосой; в мобилке `ProgressScreen.tsx` и так уже сводит пять
  прежних карточек в одну строку-очередь по срочности (`dayLine`, волна
  11). Новая механика встала туда же четвёртым по счёту кандидатом с
  нюансом приоритета: строка ритма недели (`weeklyRhythmLabel`) обгоняет
  баланс сфер, только когда ей есть что сравнивать (`rhythm.delta !== 0`);
  на её низкоинформативных заглушках («тот же ритм», «первая активность»,
  `delta===0`) баланс сфер важнее и показывается вместо неё —
  `contextBalanceLabel` возвращает `null`, если за неделю ничего не
  закрыто, и тогда мобилка тихо откатывается обратно на строку ритма.
  Отдельного блока/карточки не заведено — по тому же правилу
  `docs/UI_BUDGET.md`, что и у ритма/намерения/достижений.

### Тесты и валидация

`npx tsc --noEmit` — чисто. Ручной прогон чистых функций через
`esbuild --bundle --platform=node` + assertions в одноразовом `.cjs` (по
образцу волн 5/11): `normContext` отсекает мусор; `nextContext` — полный
цикл `null→work→home→health→null`; `computeContextBalance` — группировка
по сфере на смешанном наборе, `untagged`-корзина, отсечение дела старше 7
дней (граница: ровно 7 дней назад считается, 8 — нет), исключение
незавершённых и дел без `completedDay`; `contextBalanceLabel` — `null` при
пустом балансе, отдельный текст «без сферы», когда всё сделанное без
тега. Все проверки прошли (`ALL OK`), временные файлы удалены,
`git status --short` после прогона — только пять целевых файлов правки.

`git status --short` до начала работы — чисто (чужого незакоммиченного не
было). Коммит `1769814`, автор `Optimizator Dnya Bot`
(`optimizator.dnya@users.noreply.github.com`), запись «Автор изменений:
OS 3.3». Запушено в `optimizator-dnya-mobile@main`
(`c19c0cc..1769814`).

Живьём на телефоне/эмуляторе не гонял — только `tsc`+ручной прогон чистых
функций, как во всех прежних волнах мобилки без доступа к устройству.

Следующий пункт веб-бэклога по убыванию ценности (кандидат №4, см. выше) —
«Клавиатура, одна рука, полный a11y-проход».

## Волна 23 — недельный Peak-End отдельно от дневной аффирмации (2026-07-27, OS 3.3)

Кандидат из синтеза «интуитивный UX» ([[intuitive_ux_billion_ideas]], п.13):
дневная аффирмация (волна 19) даёт итог за один день, но нет отдельной
итоговой оценки недели целиком (принцип Peak-End Канемана — вспоминаем не
среднее, а пик и концовку). Файл: только `app.html`. `SCHEMA`/`sw.js` не
менялись — новых полей хранения нет, только чтение уже существующего `dlog`
(`readDayLog`, `{load, done, total}` по дням).

### Что сделано

- **`weekPeakEnd(dlog, todayStr)`** — чистая функция: суммирует `done`/`total`
  по 7 дням назад от `todayStr`, считая только дни, где `total>0` (пустые
  дни не портят процент); при отсутствии данных за окно возвращает `null`.
  Процент закрытых дел делит на три пула формулировок (`WEEK_PEAK_HIGH`
  ≥80%, `WEEK_PEAK_MID` 40–79%, `WEEK_PEAK_LOW` <40%).
- **Детерминированная ротация вместо `Math.random()`** — индекс фразы
  внутри пула берётся из `pct % pool.length`, поэтому повторный показ той
  же недели (тот же `dlog`/`todayStr`) всегда даёт ту же фразу — тот же
  принцип, что уже применён к `dayAffirmation` (волна 19), не новый паттерн.
- **`renderWeekPeak()`** — рендерит блок `#weekPeak` (заголовок «Итог
  недели: N из M (P%)» + фраза) под баланс сфер жизни (`ctxBalance`, волна
  22); блок `hidden`, если `weekPeakEnd` вернул `null` (первая неделя без
  данных). Вызывается из общего цикла обновления статистики, рядом с
  `renderCtxBalance()`.
- **Не новая карточка сверх бюджета** — переиспользует уже существующую
  секцию статистики (`.stats`), не заводит отдельный `<section>` верхнего
  уровня.

### Самотесты

Новая группа из 3 проверок: high-пул на 100% с отсечением дня с `total=0`;
разделение mid/low на границе 40%; `null` при полном отсутствии дней с
`total>0` в окне (в т.ч. пустой `dlog`). Итог: `?selftest=1` — `OK 67/67`
(было 64/64).

### Проверено

headless Chrome (`node w11_selftest.js` из `C:\Users\HADJAL`) на
`app.html?selftest=1` — заголовок `OK 67/67`, консоль чистая. `git status
--short` до коммита — только `app.html`. Коммит `2d2e81e`, автор
`HADJAL001`, запушено в `superday-site@main`.

Следующий пункт по этому же синтезу для SUPER DAY — визуальный (не только
текстовый) фидбек в `ux.js` синхронно с `urgentHint`/`importantHint`/
`keysHint`.

## Волна 24 — визуальный отклик синхронно с текстовыми хинтами в `ux.js` (2026-07-27, OS 3.3)

Следующий пункт из синтеза «интуитивный UX» ([[intuitive_ux_billion_ideas]],
п.10, принцип 5 «немедленный конкретный фидбек вместо абстрактного текста»).
Подсказки `urgentHint`/`importantHint` (дашд-чипы под полем ввода при
обнаружении слов-маркеров «срочно»/«важно» и т.п.) были только текстом —
глаз, задержавшийся на самом поле ввода, их не видел без взгляда вниз.
Файлы: только `ux.js` (+ `sw.js` — бампнута версия кэша, т.к. `ux.js`
входит в `SHELL`). `app.html` не менялся.

### Что сделано

- Переиспользован уже существующий в приложении язык цвета квадрантов
  матрицы (`--q1` срочное+важное, `--q2` важное) — не выдуман новый.
- Чип-подсказка получила цветную точку (`.ux-chip-dot`, `aria-hidden`):
  красноватая для `urgentHint`, янтарная для `importantHint`.
- Поле `#taskInput` получает мгновенную цветную рамку (`box-shadow: inset
  3px 0 0 0 var(--q1|--q2)`) синхронно с той же логикой обнаружения, что
  уже строит чипы (`suggUrgent`/`suggImportant` — детект вынесен в
  переменные, а не задублирован); рамка снимается вместе с исчезновением
  подсказки (текст стёрт, дело добавлено, флажок и так уже проставлен).
  `#taskInput{transition:box-shadow .15s ease}` отключается в
  `prefers-reduced-motion: reduce`.
- `keysHint` (статичная подсказка про `/`/`?`) визуальной пары не
  получила — она не событийная, добавлять анимацию/цвет было бы шумом без
  пользы.

### Проверено

`node w11_selftest.js` — `OK 67/67`, консоль чистая (у `ux.js` нет
собственных `T(...)`-тестов, интеграция app.html не пострадала). Отдельно
точечная проверка headless Chrome (Puppeteer, статика на localhost:8791):
ввод «позвонить маме срочно» → `#taskInput.style.boxShadow ===
"inset 3px 0 0 0 var(--q1)"`, `.ux-chip-dot` с `background: var(--q1)`,
без ошибок в консоли. Коммит `b9ef0a6`, автор `HADJAL001`, запушено в
`superday-site@main`.

Пункт 10 синтеза закрыт. Оставшиеся низкоприоритетные кандидаты того же
списка (11-13, кроме уже закрытого 13) — без явного запроса пользователя
не начинать.

## Волна 25 — заголовки «Намерение дня»/«Новый день»: иконка + шеврон вместо голого текста (2026-07-27, OS 3.3)

Правка была сделана в этой же сессии, но осталась незакоммиченной до
перерыва (обнаружена как чистый, самосогласованный `git diff` перед
началом следующей задачи — проверено по стандартному протоколу «не
считать чужую/непонятную незакоммиченную работу осиротевшей», здесь
репозиторий одноинстансный, так что это гарантированно собственная
недокоммиченная работа, не чужая).

### Что сделано

- Заголовки `.ro-title` в `renderIntentionBar()` и `showRollover()`
  («Намерение дня», «Новый день ✨») получили иконку слева и — для уже
  установленного намерения — кликабельный шеврон справа. Переиспользован
  уже существующий в файле паттерн `.pg-ic`/`.pg-title`/`.pg-chev` (из
  другого раздела, «Под капотом»), а не выдуман новый.
- «Уже установленное намерение» — теперь целиком кликабельная кнопка
  `.ro-head-btn` (вместо отдельной ссылки «Изменить» ниже текста):
  `openIntentionEditor()` вынесен в отдельную функцию, чтобы не
  дублировать обработчик между кнопкой-заголовком и старой кнопкой
  «Изменить» (она осталась как есть).
- Чипы выбора намерения (`.in-chips`) — `display:flex` → `display:grid;
  grid-template-columns:repeat(2,1fr)`, на мобильном брейкпоинте обратно
  в одну колонку — компактнее при длинных вариантах, меньше рваных строк.

### Проверено

`node w11_selftest.js` из `C:\Users\HADJAL` — заголовок `OK 67/67`,
консоль чистая (изменения только в разметке/CSS, встроенные `T(...)`
тесты логики намерения/rollover не затронуты). Коммит `5bd63c9`, автор
`HADJAL001`, запушено в `superday-site@main`.

## Волна 26 — честная атрибуция угаданной длительности в `.ics`-экспорте (2026-07-27, OS 3.3)

Довесок к [[intuitive_ux_billion_ideas]], п.12 (честная атрибуция оценок
за пределами уже существующего образца — `route.km по прямой, без учёта
дорог` в `app.html:1829` для fallback-маршрута). Найден исчерпывающим
поиском (`grep` по `оцен|прогноз|≈|примерно|estimate|среднее` и смежным
паттернам): единственный реальный кандидат — экспорт в `.ics`-календарь.
Для дел без указанной длительности `icsEnd()` молча подставляет
`ICS_DEFAULT_DUR = 30` минут — календарь получает точный `DTEND`, будто
длительность известна, хотя это угадано. Пользователь при экспорте не
предупреждён.

### Что сделано

- `buildICS()`: во время сборки `timed`-списка считается `defaultDur` —
  сколько дел со временем не прошли `validDur(t.dur)` (включая `dur:0` —
  тоже угадано, не введено осмысленно). Число возвращается в `counts`
  рядом с уже существующими `timed`/`repeating`/`untimed`.
- `exportICS()`: если `counts.defaultDur > 0`, к итоговому toast
  добавляется честная оговорка — «Из них N без длительности — календарь
  поставил 30 мин по умолчанию.» — по аналогии с уже существующими
  `bits.push(...)` для остальных счётчиков. Формулировка избегает
  склонения числительного с существительным («N без длительности», а не
  «У N дел») — проще и грамматически безопаснее на любом количестве.
- Новый `T(...)`-тест «buildICS: честный счётчик дел с угаданной
  длительностью» рядом с уже существующими тестами `buildICS` — три дела
  (с длительностью, без неё, с `dur:0`), ожидание `defaultDur===2`.

### Проверено

`node w11_selftest.js` из `C:\Users\HADJAL` — заголовок `OK 68/68`
(было 67/67, +1 новый тест), консоль чистая. Коммит `d88910a`, автор
`HADJAL001`, запушено в `superday-site@main`.

Пункт 12 синтеза закрыт. Из исходной волны выбранных пользователем задач
(«Сделай интерфейс понятным дальше» → asgard пп.2/4/5 + SUPER DAY п.12)
закрыты все четыре: п.2 и п.4-остаток/п.5 — подтверждением уже
существующей работы (см. `asgard-redesign/COORDINATION.md`, PR #139), п.12
— новым кодом здесь. Триггер-фраза для продолжения инициативы в будущих
сессиях сохранена в памяти проекта (`intuitive_ux_billion_ideas.md`).

## Координация: сессия 27.07 — каталог идей «платформа №1 без конкурентов» (новая инициатива, отдельно от волн 1-26 выше)

Директива основателя (27.07.2026): вывести платформу в безусловные лидеры
рынка — код/визуал/дизайн высшего уровня + мультимедийность (видео,
анимации/микровзаимодействия, изображения/иллюстрации, голос). **Сейчас —
только разведка и синтез экспертного каталога идей, БЕЗ КОДА в этой волне.**
Пользователь предупредил, что параллельно работает второй агент Claude —
эта запись фиксирует, кто что делает, чтобы не задваивать работу.

Рабочее дерево на момент разведки (27.07, ~05:20) чистое, `main` синхронизирован
с origin, конфликтов с волнами 1-26 нет. Тема «мультимедийность» в этом
репозитории ранее не встречалась — свободна.

**Явно НЕ трогается в рамках этой инициативы:** остаток бэклога
`intuitive_ux_billion_ideas` (asgard пп.7-8) — отдельный, уже согласованный
поток, не смешивается с этим документом.

Каталог идей готовится в памяти Claude (`platform_number_one_ideas.md`) и
передаётся пользователю в чате; код по выбранным пунктам пишется отдельными
волнами, по образцу волн выше, только после явного выбора пользователя.

**Обновление 27.07:** пользователь одобрил ВСЕ 10 пунктов каталога в работу
по порядку (не только SUPER DAY — оба репозитория, см. параллельную запись в
`work/asgard-redesign/COORDINATION.md`). Для SUPER DAY в списке — п.2
(озвучка вечерней Peak-End аффирмации, Web Speech API), п.3 («карта
созвездия дня», SVG), п.9 (анонимизированный бенчмарк — отмечен в каталоге
«только по отдельному запросу», уточню статус, когда дойду по порядку).
**Пользователь параллельно запускает ВТОРОЙ инстанс Claude отдельным
триггером «Придумай идеи ещё круче»** — тот пишет только новый раунд идей
(`platform_number_one_ideas_round2.md`), код не трогает. Если увидите, что
тот инстанс всё же правит код в этом репозитории — стоп, свериться с
пользователем в чате.

**П.2 ЗАВЕРШЁН (озвучка вечерней Peak-End аффирмации) → ветка
`feat/affirmation-voice-narration`.** Новая `speakAffirmation(txt)` рядом с
`dayAffirmation` (~2700) — тот же идиом cancel-then-speak, что у
`focusSay`/`speakRemind`, `ru-RU`, гейтится существующим `isMuted()`
(отдельный тумблер не заводили, как и было решено в плане). Дедуп «уже
озвучено сегодня» через `localStorage` (`superday_affirm_spoken_date`,
сверяется с `todayKey()`) — не даёт озвучивать на каждый ре-рендер
`renderPlan()`/`renderVerdict()`, только раз в календарный день. Вызов —
прямо в блоке `renderVerdict()`, где уже рендерится `.dv-affirm` (только
когда есть намерение дня и `done===total`). `CACHE` в `sw.js` бампнут
v36→v37. Гейт: `node w11_selftest.js` → 68/68, консоль чистая. Далее —
п.3 («карта созвездия дня»).
